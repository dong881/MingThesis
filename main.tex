
\documentclass[conference]{IEEEtran}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algpseudocode}
\usepackage{svg}
\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{tabularx}
\usepackage{color}
\usepackage{booktabs}
\usepackage[encapsulated]{CJK}
\usepackage{url}
\usepackage{algorithm}
\usepackage{algpseudocode}

\begin{document}
\begin{CJK}{UTF8}{bsmi}
\title{Adaptive Advance-Time Control for nFAPI Split under Unpredictable Latency}
\author{
  \IEEEauthorblockN{Ming-Hong HSU\IEEEauthorrefmark{1}, Ray-Guang Cheng\IEEEauthorrefmark{1}, and Co-author 1\IEEEauthorrefmark{2}}
  \IEEEauthorblockA{\IEEEauthorrefmark{1}Dept. of Electronic and Computer Engineering, National Taiwan University of Science and Technology, Taiwan}
  \IEEEauthorblockA{\IEEEauthorrefmark{2}Affiliation 2}
  \IEEEauthorblockA{Email: crg@mail.ntust.edu.tw}
}

\maketitle

\begin{abstract}
% TODO: Briefly summarize the problem, method, results, and contribution.
\end{abstract}

\begin{IEEEkeywords}
nFAPI, O-RAN, Functional Split, Adaptive Control.
\end{IEEEkeywords}

\section{Introduction}
\subsection{Background and Motivation}

\textcolor{blue}{The evolution of fifth-generation (5G) mobile networks toward open and disaggregated architectures has fundamentally transformed the design of Radio Access Networks (RANs). Initiatives such as Open RAN (O-RAN) and 3GPP-defined functional splits \cite{3gpp_ts_38_401} enable network functions to be distributed across heterogeneous platforms, thereby improving deployment flexibility, vendor interoperability, and cost efficiency. However, this architectural shift introduces a critical and often under-addressed challenge: \emph{maintaining precise timing coordination across physically separated network entities under non-deterministic transport conditions}.}

\textcolor{blue}{In traditional monolithic base stations, inter-layer interactions are tightly coupled within a single hardware platform, where timing relationships are inherently deterministic. In contrast, functional split architectures replace internal data buses with packet-based transport networks, where latency, jitter, and processing delays become non-deterministic \cite{larsen2019comparison}. As a result, synchronization is no longer a purely physical-layer problem, but evolves into a cross-layer system challenge involving transport protocols, scheduling logic, and timing window management.}

\textcolor{blue}{Although multiple functional split options have been standardized, including 3GPP Option 2, Small Cell Forum (SCF) Option 6 (nFAPI), and O-RAN Option 7.2x, they fundamentally differ in their synchronization models and timing sensitivities. Option 2, which separates the Packet Data Convergence Protocol (PDCP) and Radio Link Control (RLC) layers, operates over the F1 interface and primarily relies on message-based coordination and status reporting. Its timing requirements are relatively relaxed, typically at the millisecond level, and timing misalignment is mitigated through buffering and retransmission mechanisms.}

\textcolor{blue}{In contrast, Option 6 introduces a tighter coupling between the Medium Access Control (MAC) and Physical (PHY) layers through the nFAPI interface \cite{scf_222}. In this architecture, per-slot scheduling decisions must be delivered within strict deadlines, requiring slot-level synchronization between distributed entities. Synchronization is achieved through timestamp-based message exchanges (e.g., P7 Node Sync), enabling delay estimation and timing alignment. However, unlike lower-layer splits, Option 6 operates over general-purpose transport networks (e.g., Ethernet), where latency variations caused by operating system scheduling and network congestion can significantly disrupt timing determinism.}

\textcolor{blue}{Option 7.2x further pushes the limits of synchronization by splitting the PHY layer into high-PHY and low-PHY components and transporting time-sensitive in-phase and quadrature (IQ) samples over the fronthaul \cite{oran_wg4_cus}. This architecture requires nanosecond-level time and phase synchronization, typically achieved through hardware-assisted mechanisms such as IEEE 1588 Precision Time Protocol (PTP) and Synchronous Ethernet (SyncE) \cite{ieee_1588_2019}. To preserve real-time processing constraints, strict timing windows (e.g., $T_{2a}$ and $T_{a4}$) are enforced, and packets arriving outside these windows are typically discarded.}

\textcolor{blue}{These distinct synchronization paradigms also lead to fundamentally different timing window behaviors. In Option 7.2x, timing windows are rigid and enforced at the physical layer, where out-of-window packets are dropped to ensure real-time signal processing. In Option 2, timing misalignment is largely absorbed by higher-layer retransmission and buffering mechanisms, with minimal strict enforcement. Option 6, however, occupies a unique intermediate position: it requires strict slot-level timing alignment but operates over non-deterministic transport networks without hardware-enforced synchronization. Consequently, timing violations often manifest as scheduling failures (e.g., missing subframes), and existing implementations typically rely on static timing configurations that are insufficient under dynamic network conditions.}

\textcolor{blue}{The rationale for deploying the MAC layer in a cloud environment is primarily driven by resource pooling. Research demonstrates that while physical layer processing load is largely determined by bandwidth and antenna configurations, Layer 2 processing complexity—particularly the MAC scheduler—scales linearly or quadratically with the number of active User Equipments (UEs) \cite{Foukas2021_ICC, ComplexityPFS2019_WCNC}. By migrating the MAC layer to a VNF, operators can dynamically allocate computational resources to accommodate fluctuating UE densities \cite{GPP_CSCN2018}. However, this flexibility introduces endogenous latency: as UE count increases, the MAC processing time inevitably lengthens, compressing the available margin for fronthaul transmission delay. Furthermore, operating in non-ideal transport environments with elevated latency disrupts traditional Hybrid ARQ (HARQ) timelines, an issue highlighted in the O-RAN specifications \cite{oran_wg4_cus_v02}. While Option 6 inherently tolerates higher latency by separating the MAC and PHY layers—making it suitable for low-mobility small cell deployments without requiring expensive precise synchronization hardware—it still demands adaptive synchronization to manage the variable processing and transport delays.}

\textcolor{blue}{This gap highlights a fundamental limitation in current disaggregated RAN designs: while synchronization mechanisms exist at both extremes (message-level and hardware-level), there is a lack of adaptive timing control mechanisms for intermediate-layer splits operating under non-ideal transport environments.}

\subsection{Related Works}
\textcolor{blue}{The evolution of Radio Access Network (RAN) architectures from Distributed RAN (D-RAN) to Open RAN (O-RAN) requires a thorough study of functional splits \cite{larsen2019comparison, PerezRomero2023, 11124199}. The MAC-PHY split (Option 6), standardized by the Small Cell Forum (SCF) as nFAPI \cite{SCF225, scf_222}, is suitable for small cell deployments. It allows the MAC layer to be virtualized on commercial servers while the PHY layer remains on dedicated hardware.}

\textcolor{blue}{The performance of the nFAPI split depends on the transport network. Practical deployments often use non-ideal fronthaul, such as Shared Ethernet or DOCSIS. Mehran and MacKenzie \cite{Mehran2020} demonstrated that uplink throughput drops to zero with a small increase in fronthaul delay. Furthermore, server processing delay is unpredictable. Khalaf \textit{et al.} \cite{Khalaf2024} indicated that server delay increases under high CPU utilization, causing the MAC scheduler to miss its timing window.}

\textcolor{blue}{To address fronthaul latency, Huang and Zhang \cite{Huang2026} proposed a distributed MAC scheduling scheme. While this scheme reduces the impact of delayed grants, it introduces a ``split-brain'' issue with multiple schedulers. Akgun \textit{et al.} \cite{Akgun2024} utilized intelligent scheduling to adapt modulation based on interference, but did not solve the sub-millisecond timing of message delivery itself. Existing OpenAirInterface (OAI) reference models typically rely on static timing windows, leading to frequent ``missing subframe'' errors when network jitter exceeds configured limits. This paper bridges this gap by proposing a centralized Adaptive Advance-Time Control algorithm to dynamically manage scheduling timing.}

\subsection{Research Scope and Contributions}

\textcolor{blue}{In this paper, we focus on the nFAPI-based Option 6 split as a representative case of timing-sensitive yet transport-flexible RAN disaggregation. While Option 6 offers significant advantages in terms of reduced fronthaul bandwidth and deployment flexibility, its performance is highly sensitive to non-deterministic latency introduced by transport networks and server processing.}

\textcolor{blue}{We identify that the root cause of performance degradation in such systems lies in the inability of the MAC scheduler to adapt its transmission timing to dynamic end-to-end latency variations. Existing open-source implementations, such as OpenAirInterface (OAI), typically employ static timing advance configurations, which fail to accommodate fluctuating network conditions, leading to frequent synchronization failures and throughput degradation.}

\textcolor{blue}{To address this issue, we propose an adaptive timing control framework that dynamically adjusts scheduling lead time based on real-time latency estimation. The proposed approach introduces a dual-loop architecture, where a convergence optimization loop estimates end-to-end delay characteristics, and a timing actuator dynamically compensates for timing offsets to ensure that scheduling messages arrive within the valid timing window.}

\textcolor{blue}{The main contributions of this paper are summarized as follows:}

\begin{itemize}
\item \textcolor{blue}{\textbf{Systematic Analysis of Timing Uncertainty in Split 6:} We characterize the impact of non-deterministic latency, including network jitter and processing delay, on synchronization stability in disaggregated RAN systems.}
\item \textcolor{blue}{\textbf{Adaptive Advance-Time Control Algorithm:} We propose a dynamic timing adjustment mechanism that continuously aligns scheduling decisions with varying transport delays, effectively mitigating timing violations.}
\item \textcolor{blue}{\textbf{Experimental Validation on OAI Platform:} We implement the proposed framework in a real-world testbed and demonstrate that it eliminates missing subframe errors and significantly improves throughput stability under non-ideal fronthaul conditions.}
\end{itemize}

\section{System Model}
\label{sec:system_architecture}

\textcolor{blue}{Before diving into the system details, this study establishes a fundamental assumption: currently, there is a lack of Open Radio Access Network (O-RAN) Radio Units (O-RUs) in the market that natively support the Network Functional Application Platform Interface (nFAPI) via Split~6. As a result, the proposed system adopts a resilient two-stage split architecture. The network first connects via the Split~6 nFAPI interface between the network functions, and then translates to the standard O-RAN Split~7.2x interface to connect to the remote O-RU. Figure~\ref{fig:System-Architecture} illustrates this proposed system architecture and its delay management mechanism.}

\subsection{End-to-End Architecture}
\label{subsec:A_end_to_end}
\textcolor{blue}{The system establishes a complete 5G end-to-end communication link, extending from the core to the user equipment:
\begin{itemize}
\item \textbf{Core Network (CN):} The central element responsible for routing, session management, and authenticating user data.
\item \textbf{O-RAN Central Unit (O-CU):} A logical node that handles higher-layer protocol stacks, such as the Radio Resource Control (RRC) and Packet Data Convergence Protocol (PDCP). It connects with distributed units via the F1 interface.
\item \textbf{O-RAN Distributed Unit (O-DU):} To optimize computational performance and deployment flexibility, the system separates the O-DU into an O-DU High and an O-DU Low. The O-DU High is executed as a Virtualized Network Function (VNF) on a virtualized machine, whereas the O-DU Low is deployed as a Physical Network Function (PNF) on dedicated hardware.
\item \textbf{Switch:} A commercial network switch is located between the VNF and PNF. This mid-layer connection utilizes the nFAPI protocol. However, traversing this packet-switched network inevitably generates non-deterministic latency.
\item \textbf{O-RAN Radio Unit (O-RU):} A specialized hardware unit responsible for low-level physical layer (Low PHY) processing, digital beamforming, and Radio Frequency (RF) transmission.
\item \textbf{Commercial Off-The-Shelf User Equipment (COTS UE):} Standard commercial smartphones or modems that act as the terminal receivers in the network.
\end{itemize}}

\begin{figure}[!t]
  \centering
  \includegraphics[width=1\columnwidth]{figures/System Architecture.png}
  \caption{System architecture and delay management mechanism.}
  \label{fig:System-Architecture}
\end{figure}

\subsection{O-DU High: MAC and Delay Manager}
\label{subsec:B_odu_high}
\textcolor{blue}{Within the Media Access Control (MAC) layer of the O-DU High, the core components handling timing mechanisms include the Scheduler and the nFAPI Delay Manager:
\begin{itemize}
\item \textbf{Scheduler:} Critical for allocating the available radio resources (such as time-frequency resource blocks) and determining the exact priority and timing of downlink/uplink data transmissions.
\item \textbf{nFAPI Delay Manager:} This resides as the crucial control unit of this delay-aware architecture. Because unpredictable, unstable latency exists in the switch between the VNF and PNF, the Delay Manager continuously receives timing feedback from the lower physical layer. It uses this to dynamically calculate network jitter and proactively trigger the scheduler. This calculation guarantees that the scheduling instructions (\texttt{Sched data}) are generated ahead of time to account for network delays.
\end{itemize}}

\subsection{O-DU Low: High PHY and Timing Window}
\label{subsec:C_odu_low}
\textcolor{blue}{The O-DU Low manages the higher-level baseband processing of the physical layer (High PHY). At this level, the most critical synchronization mechanism is the nFAPI PNF Timing Window:
\begin{itemize}
\item \textbf{Timing Window:} A predefined, strict time duration. When the scheduling message (specifically the \texttt{DL\_TTI.request}) sent by the O-DU High propagates across the network, it must accurately arrive and fall exactly within this Window. Otherwise, the High PHY will reject the message and fail to process the subframe data.
\item \textbf{Timing Info:} The PNF continuously monitors the exact arrival timestamp of these control messages and periodically sends this ``Timing Info'' back to the Delay Manager in the MAC layer. This establishes a closed-loop feedback control system, compensating for the unpredictable transmission latency variations between the VNF and PNF machines.
\end{itemize}}

\subsection{Key Validation Metrics}
\label{subsec:D_metrics}
\textcolor{blue}{To evaluate the stability, synchronization accuracy, and real-time performance of the proposed algorithm under non-ideal environments, this study defines the following three Key Validation Metrics, where RTT denotes the Round-Trip Time:
\begin{itemize}
\item \textbf{Ping RTT ($T_7 - T_1$):} Measures the end-to-end round-trip latency at the application layer. This metric captures the complete duration from when a packet is sent from the CN to when its return acknowledgment is received by the sender.
\item \textbf{HARQ RTT ($T_6 - T_2$):} Measures the Hybrid Automatic Repeat Request (HARQ) loop delay specifically at the MAC layer. HARQ RTT accurately reflects the feedback efficiency and operational health of the link layer.
\item \textbf{VNF-PNF Downlink Latency ($T_3 - T_2$):} Measures the one-way downlink latency accumulated from the virtualized machine to the physical machine. This metric is used to directly quantify and analyze the delay jitter impact injected by the intermediate Ethernet switch.
\end{itemize}}

\textcolor{blue}{To validate the proposed performance optimization within a realistic network environment, the following assumptions and system design constraints are established:
\begin{itemize}
\item \textbf{Predominance of O-RAN Split 7.2 in Commercial Infrastructure:} While the Small Cell Forum (SCF) has standardized the nFAPI for the MAC-PHY split (Option 6) \cite{douros2018nfapi}, current commercial-grade O-RUs primarily support Split 7.2x. This creates a barrier for direct nFAPI deployment in commercial ecosystems.
\item \textbf{Research Prototypes and FPGA-based nFAPI Implementations:} Current literature on nFAPI-compliant RUs is largely restricted to experimental prototypes or FPGA-based development boards \cite{lohith2021design, BROWN2025100408}. Open-source stacks like OpenAirInterface have incorporated 5G NR nFAPI support \cite{wang2022opensource}, yet require specialized hardware adapters to interface with mainstream RUs.
\item \textbf{Hybrid System Architecture for E2E Validation:} Given the lack of native Option 6 support in commercial O-RUs, this paper adopts a hybrid architecture (Split Option 6 + Split Option 7.2). This facilitates an interworking layer that enables the nFAPI-driven MAC/High-PHY to interact with commercial O-RUs, allowing end-to-end verification using Commercial Off-The-Shelf (COTS) UEs.
\end{itemize}}

\section{Proposed Method}
\label{sec:proposed_method}

\textcolor{blue}{This section details the proposed dynamic timing adjustment algorithm. To resolve the unstable latency during packet transmission between the PNF and VNF, we propose a Dual-Loop Timing Control Architecture.}

\subsection{Dual-Loop Architecture}
\label{subsec:dual_loop_architecture}

\textcolor{blue}{The timing control of this system adopts a ``policy-execution separation'' dual-loop architecture, ensuring that the timing advancement on the VNF side can flexibly cope with network jitter without inducing unexpected oscillations:
\begin{enumerate}
    \item \textbf{Convergence Optimization Loop (Policy):} Dynamically analyzes the statistical data reported by the PNF through the convergence optimization algorithm to determine the global target slot-ahead $s_{\text{ahead}}$.
    \item \textbf{Timing Actuator Loop (Execution):} Executed by an independent VNF timing thread. This thread is the only unit in the system capable of altering the absolute sleep time and slot propagation, translating the upper-layer policy target and low-layer microsecond phase deviations into actual timing advancement.
\end{enumerate}}

\subsection{VNF Timing Actuator}
\label{subsec:vnf_timing_actuator}

\textcolor{blue}{The core function of the \textbf{VNF Timing Actuator} is isolation and precise phase execution. Let $T_{\text{slot}}$ denote the slot duration. The target slot-ahead value, $S_{\text{next}}$, is determined by the policy loop. For microsecond-level phase correction, the actuator calculates the next absolute wake-up time, operating within a mutex-locked environment to mitigate long-term error accumulation. By adjusting the absolute wake-up time, it accurately translates the upper-layer policy target and low-layer microsecond phase deviations into actual timing advancement.}

\subsection{Convergence Optimization Policy}
\label{subsec:convergence_optimization}

\textcolor{blue}{To counteract fluctuating network latency and maintain scheduling robustness, we propose a dynamic convergence optimization policy, as condensed in Algorithm~\ref{alg:delay_manager_ewma}. Upon receiving the timing statistics reported by the PNF, the system incorporates an Exponentially Weighted Moving Average (EWMA) model to estimate the mean latency ($mean$) and jitter variance ($var$).}

\textcolor{blue}{The algorithm detects statistical anomalies when the latency deviation exceeds three times the variance. If a severe jitter anomaly occurs or the late arrival exceeds the slot boundary ($T_{\text{slot}}$), the system enters ``Panic Mode.'' To balance robustness and minimal latency, we employ an asymmetric response: a \textbf{Fast up} mechanism that radically increases the slots ahead to ensure connection continuity during panic events, and a \textbf{Proactive Slow Decay} backoff. The slow decay utilizes a leaky bucket counter to filter out pseudo-stable states, cautiously reducing the delay margin only during sustained stable periods. This effectively prevents ping-pong effects and oscillation in timing adjustments.}

\begin{algorithm}
\caption{Delay Manager: Statistical Variance-Based Control}
\label{alg:delay_manager_ewma}
\begin{algorithmic}[1]
\Require 
    \Statex $t_{late}$: The latest arriving timing offset from PNF
    \Statex $S_{curr}$: Current slots ahead (trigger timing)
\Ensure 
    \Statex $S_{next}$: Proposed slots ahead for the next MAC scheduler

\Statex \textbf{/* Phase 1: EWMA Estimation */}
\State $Delay\_Deviation \gets t_{late} - mean$
\State $mean \gets mean + (Delay\_Deviation / 8)$
\State $var \gets var + (|Delay\_Deviation| - var) / 4$

\Statex \textbf{/* Phase 2: Detect "Near out of Window" */}
\State $Statistical\_Anomaly \gets |Delay\_Deviation| > (3 \times var)$
\State $Panic\_Mode \gets Statistical\_Anomaly \textbf{ or } (t_{late} \ge T_{slot})$

\Statex \textbf{/* Phase 3: Fast up \& Proactive Slow Decay */}
\If{$Panic\_Mode$}
    \State $S_{next} \gets S_{curr} + N_{jump}$ \Comment{\textbf{Fast up:} Jump slots to ensure survival}
    \State $Leaky\_Bucket\_Counter \gets Penalty\_Threshold$ \Comment{Reset decay counter}
\ElsIf{$t_{late} \le -Safe\_Boundary$}
    \If{$Leaky\_Bucket\_Counter > 0$}
        \State $Leaky\_Bucket\_Counter \gets Leaky\_Bucket\_Counter - 1$ \Comment{Stable period decay}
        \State $S_{next} \gets S_{curr}$ \Comment{Prevent ping-pong effect}
    \Else
        \State $S_{next} \gets S_{curr} - 1$ \Comment{\textbf{Proactive Slow Decay:} Minimize RTT}
        \State $Leaky\_Bucket\_Counter \gets Normal\_Threshold$
    \EndIf
\Else
    \State $S_{next} \gets S_{curr}$ \Comment{Maintain current timing}
\EndIf

\State $S_{next} \gets \max(1, \min(S_{next}, Max\_Slots))$ \Comment{Enforce system bounds}
\State \Return $S_{next}$
\end{algorithmic}
\end{algorithm}

\section{Experimental Results}
\label{sec:experiments}

\textcolor{blue}{To validate the hypothesis and the effectiveness of the proposed dynamic trigger scheduler algorithm, we systematically designed multiple experimental scenarios. Section~\ref{subsec:experimental_scenario} outlines the general software and hardware environment used across all tests. Subsequent sections present specific scenarios: Scenario I (\ref{subsec:scenario_1}) isolates and verifies the problems caused by non-deterministic latency under standard OpenAirInterface implementations, while Scenario II (\ref{subsec:scenario_2}) and subsequent experiments evaluate the robustness of our proposed time-advance control under varying levels of congestion.}

\subsection{Experimental Scenario}
\label{subsec:experimental_scenario}

\textcolor{blue}{The testbed environment connects a series of high-performance servers, radio units, and a commercial smartphone. The detailed software and hardware configurations for the end-to-end network deployment are outlined below.}

\begin{table}[!t]
\caption{Testbed Node Configuration}
\label{tab:node_config}
\centering
\begin{tabular}{l|l}
\toprule
\textbf{Unit} & \textbf{Specification} \\
\midrule
Core Network (CN) & Open5GS \\
SW Stack / Branch & OpenAirInterface (\texttt{2026.w13}) \\
VNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6226R \\
PNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6433N \\
O-RU & Pegatron RU (P5G-Indoor O-RU-PR1450) \\
COTS UE & Samsung Galaxy S20 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!t]
\caption{Wireless System Parameters}
\label{tab:wireless_params}
\centering
\begin{tabular}{l|l}
\toprule
\textbf{Parameter} & \textbf{Setting} \\
\midrule
Subcarrier Spacing & 30 kHz \\
TDD Pattern & 3D1S1U \\
MIMO / Ports & 4-layer / 4T4R \\
Fronthaul & O-RAN F release \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Scenario I: Analysis of Non-Deterministic Latency}
\label{subsec:scenario_1}

\textcolor{blue}{Deploying the VNF and PNF on different machines introduces significant challenges.
Because the MAC scheduler is separated from the PHY layer, strict slot synchronization is required between these distributed components.
However, even when deployed in the exact same network environment, different traffic loads directly impact the latency.
Figure~\ref{fig:Scheduler-to-PNF-latency} illustrates the latency between the VNF scheduler and the PNF under varying traffic loads.
As traffic increases, three factors heighten the overall delay: the scheduler requires more processing time, the data packing time lengthens, and the VNF-to-PNF One-Way Delay (OWD) is affected.
Consequently, the VNF cannot reliably transmit data exactly at the slot boundary.
Instead, it must dynamically advance its scheduling point to compensate for these varying delays and ensure packets arrive at the PNF on time.}

\begin{figure}[!t]
  \centering
  \includegraphics[width=0.8\columnwidth]{figures/Scheduler-to-PNF-latency.png}
  \caption{Observation of latency between VNF scheduler and PNF under different traffic loads.}
  \label{fig:Scheduler-to-PNF-latency}
\end{figure}



\subsection{Scenario II: [Placeholder for Future Experiment]}
\label{subsec:scenario_2}
%     X axis: 
%     Y axis:
\bibliographystyle{IEEEtran}
\bibliography{references}
\end{CJK}
\end{document}

