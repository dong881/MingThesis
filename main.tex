
\documentclass[10pt, conference, letterpaper]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage[caption=false,font=footnotesize]{subfig}
\usepackage{tabularx}
\usepackage{color}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,calc}
\usepackage{booktabs}
\usepackage{url}
\usepackage{algorithm}
\usepackage{algpseudocode}

% =========================================================================
% IEEEtran Draft/Final Color Toggle
% - Draft Mode (Default): User-added text displays in blue.
% - Final Mode: Uncomment the line below to render all blue text in black.
% =========================================================================
%\definecolor{blue}{rgb}{0,0,0} 
% =========================================================================


\begin{document}
\title{Adaptive Timing Control for Throughput–Latency Trade-off in nFAPI Split}
\author{
  \IEEEauthorblockN{Ming-Hong HSU\IEEEauthorrefmark{1}, Ray-Guang Cheng\IEEEauthorrefmark{1}, and Co-author 1\IEEEauthorrefmark{2}}
  \IEEEauthorblockA{\IEEEauthorrefmark{1}Dept. of Electronic and Computer Engineering, National Taiwan University of Science and Technology, Taiwan}
  \IEEEauthorblockA{\IEEEauthorrefmark{2}Affiliation 2}
  \IEEEauthorblockA{Email: crg@mail.ntust.edu.tw}
}

\maketitle

\begin{abstract}
\textcolor{blue}{nFAPI-based Split Option 6 separates the MAC scheduler and High-PHY across packet-switched transport, where scheduling messages must arrive at the PNF within a strict timing window. Because existing static scheduling relies on a fixed ahead time to trigger the scheduler, it cannot automatically adapt to varying network latency and jitter across different deployments. This paper proposes an adaptive timing control framework for nFAPI Split 6 that dynamically adjusts the scheduler trigger offset using PNF Timing Info feedback. The proposed method applies EWMA-based Timing Info and jitter estimation to perform adaptive timing control, adjusting the scheduling advance rapidly under timing risk and recovering it safely under stable conditions. We implement the framework in an OpenAirInterface-based end-to-end testbed with a commercial O-RU and COTS UE. Experimental results show that the proposed method maintains stable throughput under injected delay and jitter while avoiding unnecessary MAC HARQ RTT increase compared with static scheduling baselines. Additional multi-switch/router deployment results further confirm that the proposed software-based method remains effective under realistic multi-hop transport conditions.}
\end{abstract}

\begin{IEEEkeywords}
nFAPI, O-RAN, Functional Split, Adaptive Control.
\end{IEEEkeywords}

\section{Introduction}

\textcolor{blue}{Functional split architectures have been widely studied as a key enabler for flexible and cost-efficient 5G/O-RAN deployments, where different protocol functions can be distributed across centralized and distributed nodes depending on bandwidth, latency, and coordination requirements \cite{Larsen2019}, \cite{3GPP_TR_38_801}, \cite{11124199}. In nFAPI-based Split Option 6, the MAC scheduler in the VNF must send scheduling control messages sufficiently ahead of the target slot so that the PNF can process them within its timing window. If a scheduling control message arrives later than the deadline, the PNF discards the scheduling request, resulting in missing-slot events and throughput degradation.}

\textcolor{blue}{Existing OAI nFAPI-style implementations rely on statically configured \textit{slots ahead} scheduling (representing the scheduling ahead time of the VNF in units of slots). Such a static configuration creates an inherent throughput-latency trade-off: a larger slots ahead value improves robustness against delay and jitter but increases MAC HARQ RTT, whereas a smaller value reduces latency but becomes vulnerable to late packet arrivals. Since HARQ retransmission timing is tightly coupled with MAC scheduling and resource availability, excessive scheduling ahead time may inflate MAC-layer feedback latency and reduce the efficiency of HARQ process reuse \cite{Elfiky2024}. When functional split interfaces are carried over non-ideal packet-switched fronthaul or midhaul networks, variable queueing delay and transport jitter may violate the strict timing constraints imposed by lower-layer processing \cite{Diez2021}, \cite{Huang2017}, \cite{PerezRomero2023}.}

\textcolor{blue}{However, existing open-source implementations such as OAI have historically relied on simplified \texttt{slot.indication} forwarding and static slots ahead configuration, leaving the SCF-defined Timing Info based delay management process incomplete. The SCF nFAPI specification defines software-level node sync and timing info procedures to support delay management between VNF and PNF entities, enabling timing compensation without relying solely on PNF-triggered \texttt{slot.indication} messages \cite{SCF225}, \cite{SCF222}.}

\textcolor{blue}{To resolve this trade-off, this paper proposes an adaptive timing control framework for nFAPI Split 6. Our approach dynamically adjusts the scheduler trigger offset based on closed-loop feedback using PNF Timing Info reported by the PNF. By applying an Exponentially Weighted Moving Average (EWMA) model, the proposed method estimates the real-time Timing Info and transport jitter. The proposed method then applies a fast-increase and conservative-decrease policy to dynamically update the slots ahead, securing system stability under congestion while reducing latency under stable network conditions.}

\textcolor{blue}{This paper addresses the following problem: how to maintain nFAPI Split 6 scheduling stability over non-deterministic packet-switched transport without relying on hardware-level synchronization or statically configured worst-case ahead times. The key challenge is that the scheduler must choose a slots ahead value before observing the future network delay. A conservative value improves robustness but increases MAC HARQ RTT, while an aggressive value reduces latency but risks late packet arrivals and slot loss. To resolve this trade-off, we propose a closed-loop delay management method that uses PNF Timing Info to estimate the Timing Info and adapt the slots ahead value online.}

\textcolor{blue}{Recent advancements in Open RAN (O-RAN) architectures have driven significant research into enhancing split-architecture performance and managing network latency. These works can be broadly classified into three categories.}

\textcolor{blue}{\textbf{Hardware-assisted Split 7.2:} Hardware-assisted O-RAN testbeds such as X5G \cite{Villa2025} demonstrate the feasibility of high-throughput end-to-end private 5G deployments by integrating OpenAirInterface with NVIDIA ARC, GPU acceleration, and high-performance networking support. However, such designs primarily improve performance through specialized acceleration and synchronization infrastructure, whereas this work targets a software-only timing adaptation method for nFAPI Split Option 6 over general-purpose packet-switched transport.}

\textcolor{blue}{\textbf{Intra-node Software Scheduling:} Lee \textit{et al.} \cite{Lee2022} proposed an adaptive low-latency downlink transmission method for FAPI-based 5G NR gNBs, focusing on reducing intra-node processing delay within the MAC-PHY interaction. In contrast, our method addresses cross-node timing uncertainty caused by VNF-PNF transport delay and jitter in nFAPI Split Option 6. Similarly, Aquifer \cite{Aquifer2024} optimized OS-level scheduling microservices to absorb processing delay spikes, but does not handle cross-node timing violations caused by unpredictable transport delay and jitter.}

\textcolor{blue}{\textbf{nFAPI Delay Management Gap:} nFAPI has been proposed as a standard interface for 5G small cell networks \cite{Douros2018}, and open-source implementations of 5G NR Split Option 6 have demonstrated its feasibility in practical platforms \cite{Wang2022}. Nevertheless, existing implementations still largely rely on static trigger offsets, leaving the SCF-defined Timing Info based delay-management loop underexplored. Our proposed EWMA-based adaptive slots ahead adjustment method fills this critical gap, providing a cost-effective, software-only solution for COTS servers.}

\textcolor{blue}{Specifically, compared to Split 7.2 implementations like X5G \cite{Villa2025} that require hardware-level PTP synchronization and GPU/SmartNIC offloads, or FAPI-based Split 6 designs \cite{Lee2022} restricted to intra-node shared memory and process-level adjustments, our proposed method operates at the slot/TTI level on standard COTS servers, actively managing cross-node network jitter and delay over standard packet-switched Ethernet without hardware acceleration dependencies.}

\textcolor{blue}{In this paper, we focus on the nFAPI-based Option 6 split as a representative case of timing-sensitive yet transport-flexible RAN disaggregation. While Option 6 offers significant advantages in terms of reduced fronthaul bandwidth and deployment flexibility, its performance is highly sensitive to non-deterministic latency introduced by transport networks and server processing.}

\textcolor{blue}{To address the limitations of static configurations under such non-deterministic transport environments, this paper introduces a dynamic timing adjustment framework that achieves closed-loop synchronization stability. The main contributions of this paper are summarized as follows:}

\begin{itemize}
\item \textcolor{blue}{\textbf{SCF-compliant nFAPI timing framework for OAI Split 6:} We redesign the OAI nFAPI timing path to support Node Sync, PNF Timing Info, and Delay Management, enabling closed-loop timing control instead of simplified static slot forwarding.}
\item \textcolor{blue}{\textbf{EWMA-based adaptive slots ahead adjustment:} We propose a lightweight closed-loop delay-management method that estimates the mean Timing Info and jitter from PNF Timing Info and dynamically adjusts the scheduler trigger offset to balance throughput stability and MAC HARQ RTT.}
\item \textcolor{blue}{\textbf{End-to-end validation under controlled and multi-hop transport:} We implement the proposed method on an OpenAirInterface-based E2E physical testbed with a commercial Pegatron O-RU and COTS UE. We evaluate throughput, MAC HARQ RTT, missing-slot behavior, and stability under both Linux-TC-injected delay/jitter (under Experimental Scenario I) and a realistic 3-switch/3-router campus network deployment (under Experimental Scenario III). The implementation has been successfully merged into the official upstream OAI codebase to support reproducibility and future research.}
\end{itemize}

\section{System Model}
\label{sec:system_architecture}

\textcolor{blue}{Before diving into the system details, this study introduces the adopted system framework. To integrate the Network Functional Application Platform Interface (nFAPI) with mainstream commercial equipment, the proposed system adopts a stable two-stage split architecture. The network first connects via the Split~6 nFAPI interface between the network functions, and then translates to the standard O-RAN Split~7.2x interface to connect to a commercial Pegatron O-RU. Figure~\ref{fig:System-Architecture} illustrates this proposed system architecture and its delay management mechanism.}

\subsection{End-to-End Architecture}
\label{subsec:A_end_to_end}
\textcolor{blue}{The system establishes a complete 5G end-to-end communication link, extending from the core to the user equipment:
\begin{itemize}
\item \textbf{Core Network (CN):} The central element responsible for routing, session management, and authenticating user data.
\item \textbf{O-RAN Central Unit (O-CU):} A logical node that handles higher-layer protocol stacks, such as the Radio Resource Control (RRC) and Packet Data Convergence Protocol (PDCP). It connects with distributed units via the F1 interface. Together with the O-DU High, the O-CU belongs to the virtualized network functions (VNF) running on general-purpose virtual machines.
\item \textbf{O-RAN Distributed Unit (O-DU):} To optimize computational performance and deployment flexibility, the system separates the O-DU into an O-DU High and an O-DU Low. In this split, the O-DU High runs as a Virtual Network Function (VNF), whereas the O-DU Low and the underlying physical layer (Layer 1) are deployed on dedicated hardware as a Physical Network Function (PNF).
\item \textbf{Switch:} A commercial network switch is located between the VNF and PNF. This mid-layer connection utilizes the nFAPI protocol. However, traversing this packet-switched network inevitably generates non-deterministic latency.
\item \textbf{O-RAN Radio Unit (O-RU):} A specialized hardware unit responsible for low-level physical layer (Low PHY) processing, digital beamforming, and Radio Frequency (RF) transmission.
\item \textbf{Commercial Off-The-Shelf User Equipment (COTS UE):} Standard commercial smartphones or modems that act as the terminal receivers in the network.
\end{itemize}}

\begin{figure}[!t]
  \centering
  \includegraphics[width=1\columnwidth]{figures/System_Architecture.png}
  \caption{System architecture and delay management mechanism.}
  \label{fig:System-Architecture}
\end{figure}

\subsection{O-DU High: MAC, Scheduler, and Timing Core}
\label{subsec:B_odu_high}
\textcolor{blue}{Within the O-DU High virtualized network function, the timing control is organized as a unified timing control system. The core processes handling this timing core include the Scheduler, Node sync, and Delay Management:
\begin{itemize}
\item \textbf{Scheduler:} Critical for allocating the available radio resources (such as time-frequency resource blocks) and determining the exact priority and timing of downlink/uplink data transmissions.
\item \textbf{Node sync:} Responsible for software-level clock synchronization. This procedure implements a latency probe procedure by periodically exchanging synchronization timestamps ($t_1 \sim t_4$) representing microsecond-level time-reference offsets between the VNF and PNF. It calculates the instantaneous clock offset and applies slot-level ($S_{adj}$) and microsecond-level ($\mu s_{adj}$) adjustments to align the VNF's local clock to the PNF baseline, eliminating long-term hardware clock drift.
\item \textbf{Delay Management:} Operates on top of the stabilized clock phase established by Node sync. This mechanism utilizes the PNF Timing Info ($\mathit{TimingInfo}[i]$) feedback from the PNF to dynamically estimate transport network jitter. It then actively adjusts the slots ahead ($S_{\mathrm{ahead}}$) trigger offset of the scheduler. Here, \textit{slots ahead} (or \textit{VNF slots ahead}) refers to the number of slots by which the VNF triggers the scheduler in advance relative to the reference timing info. This control compensates for the non-deterministic packet transport delays and VNF processing delays without inducing synchronization loss.
\end{itemize}}

\subsection{O-DU Low: High PHY and Timing Window}
\label{subsec:C_odu_low}
\textcolor{blue}{The O-DU Low manages the higher-level baseband processing of the physical layer (High PHY). At this level, the most critical synchronization mechanism is the nFAPI PNF Timing Window:
\begin{itemize}
\item \textbf{Timing Window:} A predefined, strict time duration. Let $T_{\mathrm{window}}$ denote the duration of this PNF timing window, and $T_{\mathrm{slot}}$ denote the subframe slot duration. When the scheduling message sent by the O-DU High propagates across the network, it must accurately arrive and fall exactly within this Window. Otherwise, the High PHY will reject the message and fail to process the slot data.
\item \textbf{Timing Info ($\mathit{TimingInfo}[i]$):} The PNF continuously monitors the exact timing info of control messages relative to the transmission deadline. This information, denoted as $\mathit{TimingInfo}[i]$, represents the remaining time margin within the timing window. The PNF periodically sends this information back to the MAC layer for Delay Management. This establishes a closed-loop feedback control system, compensating for the unpredictable transmission latency variations between the VNF and PNF machines.
\end{itemize}}


\subsection{Key Validation Metrics}
\label{subsec:D_metrics}
\textcolor{blue}{To evaluate the stability, synchronization accuracy, and real-time performance of the proposed algorithm under non-ideal environments, this study defines the following Key Validation Metrics:
\begin{itemize}
\item \textbf{MAC Layer HARQ RTT ($T_5 - T_1$):} Measures the Hybrid Automatic Repeat Request (HARQ) loop delay specifically at the MAC layer. This metric accurately reflects the feedback efficiency and operational health of the link layer.
\item \textbf{VNF-PNF DL Latency ($T_2 - T_1$):} Measures the one-way downlink latency accumulated from the virtualized machine (VNF) to the physical machine (PNF). This metric is used to directly quantify and analyze the delay jitter impact injected by the intermediate Ethernet switch.
\end{itemize}
Note that RTT denotes the Round-Trip Time. To validate the split-architecture stability under non-ideal fronthaul conditions, network latency simulation is conducted at the uplink ($T_5 - T_4$) and downlink ($T_2 - T_1$) segments via Linux Traffic Control (TC).}

\textcolor{blue}{To validate the proposed performance optimization within a realistic network environment, the system design leverages a commercial-grade deployment framework:
\begin{itemize}
\item \textbf{O-RAN Split 7.2x Interoperability:} While the Small Cell Forum (SCF) has standardized the nFAPI for the MAC-PHY split (Option 6) \cite{Douros2018}, commercial-grade Open Radio Units (O-RUs) natively support the O-RAN Split Option 7.2x. To achieve seamless integration with open-source stacks like OpenAirInterface \cite{Wang2022}, our system bridges these interfaces.
\item \textbf{Hybrid System Architecture for E2E Validation:} To validate the nFAPI performance over a fully commercial ecosystem, this paper adopts a hybrid architecture (Split Option 6 + Split Option 7.2). This setup establishes a translation/interworking layer that enables the nFAPI-driven MAC/High-PHY to successfully interact with a commercial Pegatron O-RU, facilitating end-to-end verification using Commercial Off-The-Shelf (COTS) UEs.
\end{itemize}}

\section{Proposed Method}
\label{sec:proposed_method}

\textcolor{blue}{This section details the proposed dynamic timing adjustment framework. To resolve the unstable latency during packet transmission between the PNF and VNF, we propose two core timing control algorithms: Node sync for software-level clock synchronization and Delay Management for slot-level adaptive delay management. The timing control of this system is organized as a closed-loop system, ensuring that the VNF-side scheduling can dynamically track and compensate for both clock drift and slot-level transport latency variation. The two algorithms cooperate as follows:}
\begin{enumerate}
    \item \textcolor{blue}{\textbf{Node sync:} Maintains phase and slot boundary alignment between the VNF and PNF. It periodically exchanges timestamps to calculate the absolute phase offset, correcting for hardware clock drift.}
    \item \textcolor{blue}{\textbf{Delay Management:} Tracks and estimates packet transport delays and jitter at the slot level, dynamically adjusting the slots ahead ($S_{\mathrm{ahead}}$) to guarantee that control messages fall within the PNF's strict timing window.}
\end{enumerate}

\subsection{Node sync}
\label{subsec:node_sync}
\textcolor{blue}{To achieve software-level clock synchronization, the system implements a latency probe procedure compliant with the nFAPI specification. In this procedure, the VNF periodically transmits a downlink node synchronization (DL Node Sync) message containing a parameter $t_1$, which represents the transmission time offset in microseconds relative to the VNF SFN/slot 0/0 time reference. Upon receiving this message at the transport layer, the PNF records $t_2$, representing the arrival time offset in microseconds relative to the PNF SFN/slot 0/0 time reference. The PNF then immediately responds by sending an uplink node synchronization (UL Node Sync) message back to the VNF, indicating the echoed $t_1$, the recorded arrival offset $t_2$, and a parameter $t_3$ representing the transmission offset of the uplink message relative to the PNF SFN/slot 0/0 time reference. When the VNF receives the UL Node Sync message, it records a local arrival timestamp $t_4$, representing the arrival time offset in microseconds relative to the VNF SFN/slot 0/0 time reference. Because the nFAPI timestamps are constrained by a 1024 subframe number (SFN) loop, all offsets wrap around every $10.24$~seconds ($10240000$~$\mu$s). The algorithm calculates the differences $d_1 = t_2 - t_1$ and $d_2 = t_4 - t_3$, applying a wrap-around correction to align them within the interval $[-5120000, 5120000]$~$\mu$s. The VNF then calculates the instantaneous clock offset between their respective time references:}
\begin{equation}
\mathit{offset} = \frac{d_1 - d_2}{2}
\end{equation}
\textcolor{blue}{To prevent timing adjustments from causing control loop oscillations, the synchronization control loop implements a state-based locking mechanism. The system has two synchronization states: locked ($\mathit{sync\_locked} = 1$) and unlocked ($\mathit{sync\_locked} = 0$). In the locked state, the VNF monitors the drift of the local clock. If the absolute offset exceeds the slot duration $T_{\mathrm{slot}}$:}
\begin{equation}
|\mathit{offset}| > T_{\mathrm{slot}}
\end{equation}
\textcolor{blue}{the VNF unlocks the synchronization ($\mathit{sync\_locked} = 0$) to trigger re-synchronization. In the unlocked state, the VNF checks whether the offset has converged within the slot duration $T_{\mathrm{slot}}$. If $|\mathit{offset}| \le T_{\mathrm{slot}}$, the system locks the synchronization state ($\mathit{sync\_locked} = 1$). Otherwise, the VNF decomposes the offset into a slot-level adjustment $S_{\mathrm{adj}}$ and a microsecond-level adjustment $\mu s_{\mathrm{adj}}$ based on the slot duration $T_{\mathrm{slot}}$:}
\begin{equation}
S_{\mathrm{adj}} = \mathit{offset} / T_{\mathrm{slot}}
\end{equation}
\begin{equation}
\mu s_{\mathrm{adj}} = \mathit{offset} \pmod{T_{\mathrm{slot}}}
\end{equation}
\textcolor{blue}{These adjustments are accumulated into the global adjustment variables:}
\begin{equation}
\mathit{slot\_adjustment} \gets \mathit{slot\_adjustment} + S_{\mathrm{adj}}
\end{equation}
\begin{equation}
\mathit{us\_adjustment} \gets \mathit{us\_adjustment} - \mu s_{\mathrm{adj}}
\end{equation}
\textcolor{blue}{The negative sign in the microsecond adjustment reduces the local thread sleep time when the VNF clock is behind, speeding up the slot transition rate to catch up. The synchronization process is summarized in Algorithm~\ref{alg:node_sync_simplified}.}

\begin{algorithm}
\caption{Node sync}
\label{alg:node_sync_simplified}
\begin{algorithmic}[1]
\algrenewcommand\algorithmicrequire{\textbf{Input:}}
\algrenewcommand\algorithmicensure{\textbf{Output:}}
\Require $t_1, t_2, t_3$ (Timestamps from PNF \textit{UL\_node\_sync}), $t_4$ (VNF local receive timestamp)
\Ensure $\mathit{slot\_adjustment}$ (Slot adjustment accumulator), $\mathit{us\_adjustment}$ (Microsecond adjustment accumulator)

\Statex \textbf{Notation:} 
\Statex $\mathit{offset}$ (Instantaneous timing offset), $\mathit{sync\_locked}$ (Synchronization lock state)
\Statex $T_{\mathrm{slot}}$ (Slot duration in~$\mu$s), $T_{\mathrm{wrap}}$ (Wrap-around period, $10240000$~$\mu$s)
\Statex $\text{WrapCorrection}(x, T)$ (Corrects $x$ within $[-T/2, T/2]$)

\Statex \textbf{Phase I: Timing Offset Calculation}
\State $d_1 \gets \text{WrapCorrection}(t_2 - t_1, T_{\mathrm{wrap}})$
\State $d_2 \gets \text{WrapCorrection}(t_4 - t_3, T_{\mathrm{wrap}})$
\State $\mathit{offset} \gets (d_1 - d_2) / 2$

\Statex \textbf{Phase II: Hysteresis Lock and Clock Adjustment}
\If{$\mathit{sync\_locked} = 1 \land |\mathit{offset}| > T_{\mathrm{slot}}$}
    \State $\mathit{sync\_locked} \gets 0$ \Comment{Unlock due to excessive clock drift}
\EndIf

\If{$\mathit{sync\_locked} = 0$}
    \If{$|\mathit{offset}| \le T_{\mathrm{slot}}$}
        \State $\mathit{sync\_locked} \gets 1$ \Comment{Lock synchronization}
    \Else
        \State $S_{\mathrm{adj}} \gets \mathit{offset} / T_{\mathrm{slot}}$ \Comment{Integer division}
        \State $\mu s_{\mathrm{adj}} \gets \mathit{offset} \pmod{T_{\mathrm{slot}}}$
        \State $\mathit{slot\_adjustment} \gets \mathit{slot\_adjustment} + S_{\mathrm{adj}}$
        \State $\mathit{us\_adjustment} \gets \mathit{us\_adjustment} - \mu s_{\mathrm{adj}}$
    \EndIf
\EndIf
\State \Return $\mathit{slot\_adjustment}, \mathit{us\_adjustment}$
\end{algorithmic}
\end{algorithm}

\subsection{Delay Management}
\label{subsec:delay_management}

\textcolor{blue}{To counteract changing network latency and maintain scheduling stability, a dynamic delay-management strategy is implemented. This strategy operates as a closed-loop feedback mechanism based on standard Small Cell Forum (SCF) PNF Timing Info reported by the PNF. By evaluating the Timing Info relative to the PNF deadline, the proposed method dynamically adjusts the scheduling ahead time $S_{\mathrm{ahead}}$.}

\subsubsection{Timing Model}
\textcolor{blue}{Let $T_{\mathrm{slot}}$ denote the slot duration and $S_{\mathrm{ahead}}$ denote the number of slots by which the VNF scheduler triggers earlier than the target slot. For a scheduling message generated at VNF time $t_g$, the corresponding target PNF processing deadline is determined by the target slot boundary. The Timing Info reported by the PNF is denoted by $\mathit{TimingInfo}[i]$, where $\mathit{TimingInfo}[i] < 0$ indicates that the message arrives before the deadline, and $\mathit{TimingInfo}[i] > 0$ indicates a late arrival. Therefore, the delay-management objective is to select the minimum stable $S_{\mathrm{ahead}}$ such that late arrivals are avoided ($\mathit{TimingInfo}[i] \le 0$) while MAC HARQ RTT is not unnecessarily inflated.}

\subsubsection{EWMA-based Timing-Info Estimation}
\textcolor{blue}{When functional splits are deployed over packet-switched networks, the jitter of packet latency varies a lot with the network load. To address this, the proposed method references the congestion avoidance and control algorithm proposed by Jacobson and Karels~\cite{jacobson1988congestion} to introduce an Exponentially Weighted Moving Average (EWMA) filter that tracks both the mean and the deviation of the timing info. This filter smooths sudden network jitter, and its weight parameters can be changed into bitwise shift operations to reduce the computing overhead in the RAN underlayer~\cite{jacobson1988congestion}. Specifically, an EWMA filter is used to estimate the mean late arrival $\mathit{TimingInfoEWMA}[i]$ and the arrival jitter deviation $\mathit{TimingInfoDev}[i]$ based on the observed PNF Timing Info $\mathit{TimingInfo}[i]$ at slot $i$. The mean estimation is updated as:}
\begin{small}
\begin{equation}
\begin{split}
\mathit{TimingInfoEWMA}[i] & = (1 - \alpha) \mathit{TimingInfoEWMA}[i-1] \\
& \quad + \alpha \mathit{TimingInfo}[i]
\end{split}
\end{equation}
\end{small}
\textcolor{blue}{where the smoothing factor is chosen as $\alpha = 1/8$ to track the long-term latency trend, and its initial value is set to the first observed Timing Info value.}

\textcolor{blue}{The jitter deviation $\mathit{TimingInfoDev}[i]$ is updated to capture timing variation as:}
\begin{small}
\begin{equation}
\begin{split}
\mathit{TimingInfoDev}[i] & = (1 - \beta) \mathit{TimingInfoDev}[i-1] \\
& \quad + \beta |\mathit{TimingInfo}[i] - \mathit{TimingInfoEWMA}[i]|
\end{split}
\end{equation}
\end{small}
\textcolor{blue}{where $\beta = 1/4$ is the deviation smoothing factor, and its initial value is set to half of the absolute first observed Timing Info value. To design the decision thresholds for the slots ahead, we use the network latency estimation parameters defined in the IETF RFC 6298 standard~\cite{rfc6298}. Specifically, we set the mean gain to $\alpha = 1/8$ and the deviation gain to $\beta = 1/4$, and use four times the deviation as a safety margin~\cite{rfc6298}. This parameter combination is a standard that has been tested in the industry for a long time. It covers most extreme network delays in a safe and stable way, ensuring that the nFAPI Split 6 architecture keeps strict timing synchronization and system stability under unpredictable jitter. Both parameters are chosen as negative powers of two ($\alpha = 2^{-3}$, $\beta = 2^{-2}$), which allows efficient fixed-point implementation using bitwise shift operations in the timing-critical MAC path.}\subsubsection{Adaptive Slots Ahead Control}
\label{subsubsec:delay_control}
\textcolor{blue}{The slots ahead trigger timing $S_{\mathrm{ahead}}$ is dynamically updated using a fast-increase and conservative-decrease control law. The adjustment logic is executed upon receiving each Timing Info report.}

\textcolor{blue}{The decision rules are defined as follows:}
\begin{itemize}
    \item \textbf{Rapid Increase (Preserve Throughput):} \textcolor{blue}{If the smoothed worst-case arrival estimate (representing timing risk $\mathit{TimingInfoEWMA}[i] + \mathit{TimingInfoDev}[i]$) crosses the deadline ($\mathit{TimingInfoEWMA}[i] + \mathit{TimingInfoDev}[i] > 0$), the slots ahead value is immediately increased to prevent late packet arrivals and slot dropouts:}
          \begin{small}
          \begin{equation}
          S_{\mathrm{next}} = S_{\mathrm{curr}} + \left\lceil \frac{\mathit{TimingInfoEWMA}[i] + \mathit{TimingInfoDev}[i]}{T_{\mathrm{slot}}} \right\rceil
          \end{equation}
          \end{small}
    \item \textbf{Gradual Decrease (Reduce Latency):} \textcolor{blue}{If the estimated latency shows a safe margin such that the worst-case arrival time is early enough, i.e., $\mathit{TimingInfoEWMA}[i] + 4 \times \mathit{TimingInfoDev}[i] < -T_{\mathrm{slot}}$, the trigger timing is decremented by one slot to reduce latency:}
          \begin{equation}
          S_{\mathrm{next}} = S_{\mathrm{curr}} - 1
          \end{equation}
    \item \textbf{Maintain Baseline:} \textcolor{blue}{Otherwise, the trigger timing remains unchanged:}
          \begin{equation}
          S_{\mathrm{next}} = S_{\mathrm{curr}}
          \end{equation}
\end{itemize}
\textcolor{blue}{To ensure the trigger timing remains within valid operational boundaries, the adjusted value is clamped to the range $[1, \lfloor T_{\mathrm{window}} / T_{\mathrm{slot}} \rfloor]$:}
\begin{equation}
S_{\mathrm{next}} \gets \max\left(1, \min\left(S_{\mathrm{next}}, \left\lfloor \frac{T_{\mathrm{window}}}{T_{\mathrm{slot}}} \right\rfloor \right)\right)
\end{equation}
\textcolor{blue}{where the trigger timing is bounded by 1 and the maximum number of slots in the timing window $\lfloor T_{\mathrm{window}} / T_{\mathrm{slot}} \rfloor$.}

\textcolor{blue}{The core delay management strategy is condensed in Algorithm~\ref{alg:delay_management}.}\begin{algorithm}
\caption{Delay Management}
\label{alg:delay_management}
\begin{algorithmic}[1]
\algrenewcommand\algorithmicrequire{\textbf{Input:}}
\algrenewcommand\algorithmicensure{\textbf{Output:}}
\Require $\mathit{TimingInfo}[i]$ (Observed PNF Timing Info), 
\Statex \hskip\algorithmicindent $S_{\mathrm{curr}}$ (Current trigger timing), 
\Statex \hskip\algorithmicindent $T_{\mathrm{window}}$ (Timing window), $T_{\mathrm{slot}}$ (Slot duration)
\Ensure $S_{\mathrm{next}}$ (Trigger timing for the next MAC schedule)
\Statex 
\Statex \textbf{Notation:} 
\Statex $\mathit{TimingInfoEWMA}[i]$, $\mathit{TimingInfoDev}[i]$: 
\Statex \hskip\algorithmicindent smoothed mean and arrival jitter
\Statex $\alpha, \beta$: smoothing factors
 
\Statex \textbf{Phase I: Latency and Jitter Estimation}
\State $\mathit{TimingInfoEWMA}[i] \gets (1 - \alpha)\mathit{TimingInfoEWMA}[i-1]$ 
\Statex \hskip\algorithmicindent $+ \alpha \mathit{TimingInfo}[i]$
\State $\mathit{TimingInfoDev}[i] \gets (1 - \beta)\mathit{TimingInfoDev}[i-1] + {}$
\Statex \hskip\algorithmicindent $\beta |\mathit{TimingInfo}[i] - \mathit{TimingInfoEWMA}[i]|$

\Statex \textbf{Phase II: Adjustment Decision}
\If{$\mathit{TimingInfoEWMA}[i] + \mathit{TimingInfoDev}[i] > 0$}
    \State $S_{\mathrm{next}} \gets S_{\mathrm{curr}} + {}$
    \Statex \hskip 2.5em $\left\lceil \frac{\mathit{TimingInfoEWMA}[i] + \mathit{TimingInfoDev}[i]}{T_{\mathrm{slot}}} \right\rceil$ \Comment{Rapid Increase}
\ElsIf{$\mathit{TimingInfoEWMA}[i] + 4 \times \mathit{TimingInfoDev}[i] < -T_{\mathrm{slot}}$}
    \State $S_{\mathrm{next}} \gets S_{\mathrm{curr}} - 1$ \Comment{Gradual Decrease}
\Else
    \State $S_{\mathrm{next}} \gets S_{\mathrm{curr}}$ \Comment{Maintain Baseline}
\EndIf
\State $S_{\mathrm{next}} \gets \max\left(1, \min\left(S_{\mathrm{next}}, \left\lfloor \frac{T_{\mathrm{window}}}{T_{\mathrm{slot}}} \right\rfloor\right)\right)$ \Comment{Clamp boundaries}
\State \Return $S_{\mathrm{next}}$
\end{algorithmic}
\end{algorithm}

\subsection{Coordination between Node sync and Delay Management}
\label{subsec:coordination}

\textcolor{blue}{To ensure stable operation under real-world conditions, node sync and delay management must coordinate without inducing control loop oscillations. The microsecond-level node sync focuses strictly on correcting clock phase offsets to align the VNF's scheduling thread with the PNF's slot transitions. Because it directly shifts the VNF's time base, any clock adjustment ($S_{adj}, \mu s_{adj}$) temporarily shifts the arrival timestamp reference. Delay management is designed to absorb this transient shifting. By relying on history-weighted EWMA-based risk tracking ($\mathit{TimingInfoEWMA}[i] + \mathit{TimingInfoDev}[i]$), delay management avoids reacting to the temporary timing offsets caused by clock adjustments, allowing node sync to converge. Once node sync locks synchronization ($\mathit{sync\_locked} = 1$), the time base stabilizes, allowing delay management to achieve precise slot-level optimizations ($S_{\mathrm{ahead}}$).}

\section{Experimental Results}
\label{sec:experiments}

\textcolor{blue}{Experiments were conducted to validate the proposed method in an OpenAirInterface (OAI) nFAPI end-to-end environment. Unless specified otherwise, the default nFAPI parameters are configured with a timing window of 6000~$\mu$s, a periodic timing info mode, and a timing info period of 1~slot. OpenAirInterface has been widely used as an open-source platform for 5G NR end-to-end experimentation and system-level validation \cite{Ryu2022}, \cite{Wang2022}. Each reported throughput value represents the average of 180 data points, collected from three independent trials with 60 samples per trial.}

\textcolor{blue}{Section~\ref{subsec:experimental_scenario} and Section~\ref{subsec:software_validation} first present the testbed setup, experimental scenarios, and software validation. The subsequent subsections (Sections~\ref{subsec:scenario_1} to \ref{subsec:scenario_6}) evaluate the six key aspects of our method: (1) EWMA-based timing info stabilization, (2) throughput and MAC RTT efficiency, (3) stability under network congestion, (4) synchronization offset, (5) ping latency under varying throughput, and (6) performance under different reporting modes.}

\subsection{Experimental Scenario and rApp Framework}
\label{subsec:experimental_scenario}

\textcolor{blue}{To evaluate the performance of our proposed timing adjustment framework under different environments, we design three topologies:
\begin{itemize}
\item \textbf{Experimental Scenario I (Controlled Single-Switch environment):} A standard testbed where the VNF and PNF are connected directly via a single commercial Ethernet switch, as shown in the rApp automated evaluation framework (Fig.~\ref{fig:rapp_architecture}). This controlled setup is used to analyze EWMA filtering, benchmark throughput-latency trade-offs against static baselines, and evaluate robustness under delay and jitter. Specifically, Linux Traffic Control (TC) is applied between the nodes to inject artificial latency and jitter: on the downlink fronthaul link ($T_2-T_1$) and on the uplink MAC feedback link ($T_5-T_4$).
\item \textbf{Experimental Scenario II (Near-Site Single-Hop Deployment):} A near-site campus deployment where the VNF and PNF are co-located in the same campus area and connected via a single commercial Ethernet switch, introducing minimal base transport latency (around 0.3--0.5~ms). This scenario, together with Scenario III, is deployed in the campus network topology illustrated in Fig.~\ref{fig:scenario2_topology}. It evaluates the proposed timing control under stable, low-latency fronthaul conditions.
\item \textbf{Experimental Scenario III (Multi-Switch/Router Campus Deployment):} A realistic multi-hop campus network topology where the VNF and PNF are physically disaggregated across different campus buildings. As shown in Fig.~\ref{fig:scenario2_topology}, the connection traverses $3$ commercial switches and $3$ commercial routers, introducing realistic transport delays and route-dependent jitter (ping latency up to 2.0~ms).
\end{itemize}}

\textcolor{blue}{The detailed software and hardware configurations for the end-to-end network deployment in the testbed environment are outlined below.}

\begin{table}[!htbp]
\caption{Testbed Node Configuration}
\label{tab:node_config}
\centering
\begin{tabular}{l|l}
\toprule
\textbf{Unit} & \textbf{Specification} \\
\midrule
Core Network (CN) & Open5GS \\
SW Stack / Branch & OpenAirInterface (\texttt{2026.w13}) \\
VNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6226R \\
PNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6433N \\
O-RU & Pegatron RU (P5G-Indoor O-RU-PR1450) \\
COTS UE & Samsung Galaxy S20 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\caption{Wireless System Parameters}
\label{tab:wireless_params}
\centering
\begin{tabular}{l|l}
\toprule
\textbf{Parameter} & \textbf{Setting} \\
\midrule
Subcarrier Spacing & 30 kHz \\
TDD Pattern & 3D1S1U \\
MIMO / Ports & 4-layer / 4T4R \\
Fronthaul & O-RAN F release \\
\bottomrule
\end{tabular}
\end{table}

\textcolor{blue}{To ensure automated measurement and analysis with consistent data quality, an automated evaluation rApp was developed to operate on the Non-Real-Time RAN Intelligent Controller (Non-RT RIC) within the O-RAN Service Management and Orchestration (SMO) framework \cite{ORAN_Architecture2024}. As shown in Fig.~\ref{fig:rapp_architecture}, this setup serves as the testbed architecture for Experimental Scenario I. This rApp automates the start-up of the VNF and PNF. Once the gNB is established, it automatically connects to the control PC via SSH to toggle the UE's airplane mode using Android Debug Bridge (ADB), and subsequently uses SSH to launch the iPerf server on the Core Network (CN) server. It then executes a series of pre-scheduled iPerf tests automatically. Finally, the rApp collects all log files via O1 SFTP and utilizes Python scripts to plot the following data analysis charts. Crucially, it manages the network latency simulation by running Linux Traffic Control (TC) on the intermediate link. Specifically, Linux TC is applied to inject latency and jitter on the downlink fronthaul link ($T_2-T_1$) and on the uplink MAC feedback link ($T_5-T_4$) to systematically validate the split-architecture stability.}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.9\columnwidth]{figures/rapp.png}
  \caption{Architecture of the rApp automated evaluation framework for Experimental Scenario I (Controlled Single-Switch setup with Linux TC delay injection).}
  \label{fig:rapp_architecture}
\end{figure}

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/Experimental_Scenario.png}
  \caption{Experimental Scenario II and III: campus network deployments (near-site single-hop and multi-switch/router multi-hop) for evaluating the proposed adaptive timing control under realistic transport.}
  \label{fig:scenario2_topology}
\end{figure*}

\subsection{Thorough System-Level Integration and Validation}
\label{subsec:software_validation}

\textcolor{blue}{To prove the stability, practical deployability, and compatibility of the proposed timing control and nFAPI modifications, our software implementation was submitted and successfully merged into the official upstream OpenAirInterface (OAI) repository. Beyond our localized end-to-end evaluation using the rApp framework, the proposed solution went through thorough testing in the official OAI Continuous Integration and Continuous Deployment (CI/CD) pipeline. Specifically, the code successfully passed all image building and cross-compilation tests across standard x86 and ARM64-based architectures, including enterprise-grade Red Hat Enterprise Linux (RHEL) clusters and NVIDIA Jetson edge platforms. Furthermore, our implementation successfully completed key system-level integration and physical layer regression tests. These tests include 5G Standalone (SA) end-to-end integration, real radio frequency (RF) transmissions using USRP B200 and AW2S radio units, standard F1/N2 interface control-plane handover procedures, and 3GPP-compliant physical layer channel simulations. These regression and compilation results confirm that our proposed software-defined timing loops are highly robust, backward-compatible, and ready for diverse commercial cloud hardware deployments without introducing any hardware dependency.}

\subsection{Experiment 1: Validation of EWMA for PNF Timing Info}
\label{subsec:scenario_1}

\textcolor{blue}{This scenario validates the application of an Exponentially Weighted Moving Average (EWMA) to smooth the PNF Timing Info ($\mathit{TimingInfo}[i]$). Fig.~\ref{fig:exp1}(a) shows the timing control directly using the raw PNF Timing Info ($\mathit{TimingInfo}[i]$), where the raw timing values change a lot due to network jitter. Fig.~\ref{fig:exp1}(b) is the cumulative count of the packets exceeding the deadline when the timing adjustment directly uses the raw PNF Timing Info ($\mathit{TimingInfo}[i]$). The EWMA mechanism filters out the high-frequency jitter, as shown in the PNF Timing Info ($\mathit{TimingInfo}[i]$) after applying EWMA in Fig.~\ref{fig:exp1}(c). Finally, Fig.~\ref{fig:exp1}(d) shows the cumulative count of the packets exceeding the deadline under the EWMA-based timing control. The results show that applying EWMA allows the timing loop to adjust the slots ahead stably, which significantly reduces the number of packets that arrive after the deadline.}

\begin{figure}[!htbp]
  \centering
  \subfloat[{Direct Use of \(\mathit{TimingInfo}[i]\)}]{\includegraphics[width=0.48\columnwidth]{figures/exp1a.png}\label{fig:exp1a}}
  \hfill
  \subfloat[Cumulative Count of Packets Exceeding Deadline under Direct Use]{\includegraphics[width=0.48\columnwidth]{figures/exp1b.png}\label{fig:exp1b}}
  
  \subfloat[{EWMA Application on \(\mathit{TimingInfo}[i]\)}]{\includegraphics[width=0.48\columnwidth]{figures/exp1c.png}\label{fig:exp1c}}
  \hfill
  \subfloat[Cumulative Count of Packets Exceeding Deadline under EWMA]{\includegraphics[width=0.48\columnwidth]{figures/exp1d.png}\label{fig:exp1d}}
  \caption{Validation of EWMA processing on PNF Timing Info.}
  \label{fig:exp1}
\end{figure}

\subsection{Experiment 2: Throughput \& MAC RTT efficiency against static baselines}
\label{subsec:scenario_2}

\textcolor{blue}{We evaluate the system throughput and average MAC HARQ RTT under stable fronthaul conditions,
as shown in Fig.~\ref{fig:exp2}.
The proposed method aims to sustain the target throughput across varying offered loads while minimizing the MAC HARQ RTT.
As shown in Fig.~\ref{fig:exp2a}, the proposed method and the static 8 slots ahead configuration sustain a throughput of
about 940~Mbps at 1000~Mbps offered load.
In contrast, the 4 slots ahead and 6 slots ahead configurations level off at about 830~Mbps and 870~Mbps, and the
2 slots ahead configuration drops to near-zero throughput beyond 200~Mbps.
However, as shown in Fig.~\ref{fig:exp2b}, the static 8 slots ahead configuration has a high average MAC HARQ RTT of
about 9.2~ms.
The proposed method dynamically adjusts the scheduling advance, keeping a low RTT of 5.8~ms to 6.7~ms at lower loads
(100--700~Mbps).
Under 1000~Mbps offered load, it adjusts to a higher scheduling advance to sustain throughput, resulting in an average
RTT of about 7.9~ms.
This RTT is still lower than that of static 6, 8, and 10 slots ahead configurations, showing that the proposed method
effectively minimizes RTT overhead while sustaining the target throughput.}

\begin{figure}[!htbp]
  \centering
  \subfloat[Throughput Optimization]{\includegraphics[width=0.48\columnwidth]{figures/exp2a.png}\label{fig:exp2a}}
  \hfill
  \subfloat[MAC Latency \& Reliability]{\includegraphics[width=0.48\columnwidth]{figures/exp2b.png}\label{fig:exp2b}}
  \caption{Performance evaluation: MAC RTT efficiency and system throughput.}
  \label{fig:exp2}
\end{figure}

\subsection{Experiment 3: Stability under Traffic Control Network Congestion}
\label{subsec:scenario_3}

\textcolor{blue}{To evaluate stability, we inject delay and jitter between the VNF and PNF using Linux TC. As shown in Fig.~\ref{fig:exp3a}, static slots ahead configurations suffer from synchronization failures and throughput drops when delay exceeds the timing window, whereas our proposed adaptive mechanism dynamically increases the slots ahead to maintain stable throughput. Specifically, the proposed method maintains a throughput above 900~Mbps across all tested VNF-PNF latency values up to 3.0~ms, while the best-performing static configuration (10~slots ahead) degrades to near-zero throughput at approximately 2.5~ms injected latency. Additionally, we evaluate the UE DL RX throughput under a saturated 1~Gbps UDP offered load with unpredictable link jitter. As shown in Fig.~\ref{fig:exp3b}, while fixed slots ahead configurations experience severe throughput degradation due to late packets, the proposed adaptive method dynamically tracks jitter and secures stable, high throughput at the UE. The proposed method exhibits graceful throughput degradation, maintaining over 900~Mbps at 0--600~$\mu$s jitter and approximately 750~Mbps even at 1000~$\mu$s jitter. In contrast, the best-performing static configuration (6~slots ahead) drops below 300~Mbps at 800~$\mu$s and near zero at 1000~$\mu$s, while configurations with 4, 8, and 10~slots ahead all fail below 600~$\mu$s.}

\begin{figure}[!htbp]
  \centering
  \subfloat[Stability under Delay Congestion]{\includegraphics[width=0.48\columnwidth]{figures/exp3a.png}\label{fig:exp3a}}
  \hfill
  \subfloat[Throughput under Unpredictable Jitter]{\includegraphics[width=0.48\columnwidth]{figures/exp3b.png}\label{fig:exp3b}}
  \caption{System stability and throughput performance under varying network conditions.}
  \label{fig:exp3}
\end{figure}

\subsection{Experiment 4: Synchronization Offset}
\label{subsec:scenario_4}

\textcolor{blue}{To evaluate the synchronization accuracy of the proposed node sync algorithm under a multi-hop transport network, we deploy the VNF and PNF across the campus network in Experimental Scenario III. As shown in Fig.~\ref{fig:scenario2_topology}, the VNF-side components are placed at a remote computing site, while the radio-side components and UE remain at the local radio site. Fig.~\ref{fig:exp4} shows the measured VNF-PNF synchronization offset in microseconds over elapsed slots. When the VNF and PNF establish their connection, they start calculating slot 0 independently. Program start-up timing differences cause an initial program execution timing offset. Additionally, their clock phase drifts during operation. When the accumulated offset exceeds one slot (500~$\mu$s for subcarrier spacing of 30~kHz and slot duration of 500~$\mu$s), the system triggers the node sync algorithm to adjust the VNF's scheduling time base. This aligns the VNF's slot boundaries with the PNF, correcting the time-base offset and stabilizing the synchronization offset between 0 and 100~$\mu$s.}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\columnwidth]{figures/exp4.png}
  \caption{VNF-PNF synchronization offset over elapsed slots under the multi-switch/router deployment.}
  \label{fig:exp4}
\end{figure}

\subsection{Experiment 5: Ping Latency under Different Throughput}
\label{subsec:scenario_5}

\textcolor{blue}{We evaluate the throughput and ping latency under different real network deployment topologies. We use ping to measure the network interface latency between the VNF and PNF. Under Scenario II (Near-site deployment, Fig.~\ref{fig:exp5}(a)), the base latency is lower and more stable (around 0.3 to 0.5~ms). As illustrated in the metrics-specific line charts of Fig.~\ref{fig:exp5}(a), which effectively illustrate the performance trade-offs, the proposed method maintains stable near-peak throughput, and because the transport delay is lower, the required slots ahead parameter is smaller, which also reduces the MAC HARQ RTT. Under Scenario III (Remote deployment, Fig.~\ref{fig:exp5}(b)), the connection spans the campus network and traverses multiple switches and routers, resulting in higher base latency and queueing delay variations (ping latency up to 2.0~ms). Despite this severe transport jitter, the proposed method dynamically adapts the slots ahead parameter to prevent late packet arrivals, maintaining stable data transmission.}

\begin{figure}[!htbp]
  \centering
  \subfloat[Scenario II: Near-site deployment]{\includegraphics[width=0.48\columnwidth]{figures/exp5a.png}\label{fig:exp5a}}
  \hfill
  \subfloat[Scenario III: Remote deployment]{\includegraphics[width=0.48\columnwidth]{figures/exp5b.png}\label{fig:exp5b}}
  \caption{UE RX throughput and ping latency under different real network deployment scenarios.}
  \label{fig:exp5}
\end{figure}

\subsection{Experiment 6: Timing Info Mode}
\label{subsec:scenario_6}

\textcolor{blue}{We evaluate the impact of different timing info reporting modes and periods under Scenario II (Near-site deployment, Fig.~\ref{fig:exp6}(a)) and Scenario III (Remote deployment, Fig.~\ref{fig:exp6}(b)). The nFAPI protocol supports periodic and aperiodic timing info feedback modes. Fig.~\ref{fig:exp6}(a) and Fig.~\ref{fig:exp6}(b) present the packet loss rate under different reporting configurations for both deployment scenarios. In both cases, increasing the timing period in periodic mode leads to a higher packet loss rate. This is because a longer period reduces feedback frequency. As a result, the VNF timing loop cannot adjust the slots ahead parameter fast enough to adapt to network variations, causing more slot packets to arrive late and be discarded by the PNF. Under Scenario III (Remote deployment), the longer distance and higher transport jitter result in a higher base packet loss rate, making the system more sensitive to feedback delay. Under Scenario II (Near-site deployment), the lower base latency and jitter allow the system to maintain a lower packet loss rate even with a slightly longer timing period.}

\begin{figure}[!htbp]
  \centering
  \subfloat[Scenario II: Near-site deployment]{\includegraphics[width=0.48\columnwidth]{figures/exp6a.png}\label{fig:exp6a}}
  \hfill
  \subfloat[Scenario III: Remote deployment]{\includegraphics[width=0.48\columnwidth]{figures/exp6b.png}\label{fig:exp6b}}
  \caption{Evaluation of packet loss rate under different deployment scenarios.}
  \label{fig:exp6}
\end{figure}

\section{Conclusion}
\label{sec:conclusion}

\textcolor{blue}{This paper presented a dynamic timing adjustment framework for disaggregated RAN architectures, focusing on nFAPI-based Split Option 6 over non-ideal packet-switched networks. To address the limitations of static slots ahead configurations under unpredictable network jitter and server processing delay, the proposed method applies EWMA-based arrival-margin and jitter estimation to dynamically adjust the scheduler trigger timing. Experimental validation on the OpenAirInterface platform with a commercial Pegatron O-RU shows that the proposed method reduces synchronization failures and maintains stable throughput under severe delay and jitter conditions. Compared with hardware-centric approaches that rely on specialized acceleration or precise synchronization infrastructure, the proposed software-based method provides a cost-effective and flexible alternative for robust O-RAN deployment on general-purpose COTS servers.}

\textcolor{blue}{Although the proposed software-based timing control achieves robust performance, the current evaluation focuses primarily on downlink timing control under controlled delay and jitter injection. Future work will extend the proposed method to timing enhancements, multi-cell deployments, and analytical stability bounds for the feedback loop.}

\bibliographystyle{IEEEtran}
\bibliography{references}
\end{document}

