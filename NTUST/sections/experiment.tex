\documentclass[../my_ntust_thesis.tex]{subfiles}
\begin{document}
\iffalse
\chapter{實驗結果}
\fi
\chapter{Experimental Results}
\label{sec:experiment}

This study conducts experiments in an OpenAirInterface (OAI) nFAPI end-to-end environment to verify the proposed delay management method. To ensure the statistical reliability of the evaluation, each throughput value reported represents the average of 180 data points (3 independent trials $\times$ 60 samples per trial). Unless specified otherwise, the default nFAPI parameters used across all evaluations are configured with a timing window of 6000~$\mu$s, a periodic timing info mode, and a timing info period of 1~slot.

To evaluate the performance of our proposed closed-loop synchronization and delay management algorithms, the experiments are designed to verify several key aspects of the system. First, we examine whether the EWMA-based filtering effectively stabilizes the noisy PNF timing info feedback under transport network jitter. Second, we evaluate whether the adaptive slots ahead method can maintain stable throughput while minimizing unnecessary MAC HARQ RTT compared with static baselines. Third, we test if the proposed closed-loop delay management preserves end-to-end synchronization stability and throughput under severe network congestion and dynamic jitter. Fourth, we verify whether the node sync algorithm dynamically corrects the time-base offset and maintains alignment under a multi-hop campus network. Fifth, we evaluate the ping latency under different throughput and real deployment scenarios. Finally, we study the impact of different timing info reporting modes and periods on the packet loss rate under different transport distances.

The rest of this chapter is organized as follows: Section~\ref{subsec:experimental_scenario} outlines the hardware, software, and the automated rApp evaluation framework, defining Experimental Scenario I, Experimental Scenario II, and Experimental Scenario III. Section~\ref{subsec:scenario_1} presents the validation of the EWMA filter under Experiment~1: Validation of EWMA for PNF Timing Info. Section~\ref{subsec:scenario_2} evaluates the throughput and latency performance under Experiment~2: Throughput \& MAC RTT efficiency against static baselines. Section~\ref{subsec:scenario_3} validates the closed-loop delay management stability under Experiment~3: Stability under Traffic Control Network Congestion. Section~\ref{subsec:scenario_4} evaluates the node sync accuracy under Experiment~4: Synchronization Offset. Section~\ref{subsec:scenario_5} assesses the ping latency under Experiment~5: Ping Latency under Different Throughput. Finally, Section~\ref{subsec:scenario_6} analyzes the impact of reporting modes and periods under Experiment~6: Timing Info Mode.

\iffalse
\section{實驗情境}
\fi
\section{Experimental Scenario}
\label{subsec:experimental_scenario}

To evaluate the performance of our proposed timing adjustment framework under different environments, we design three topologies:
\begin{itemize}
\item \textbf{Experimental Scenario I (Controlled Single-Switch environment):} A standard testbed where the VNF and PNF are connected directly via a single commercial Ethernet switch, as shown in the rApp automated evaluation framework (Figure~\ref{fig:rapp_architecture}). This controlled setup is used to analyze EWMA filtering, benchmark throughput-latency trade-offs against static baselines, and evaluate robustness under delay and jitter. Specifically, Linux Traffic Control (TC) is applied between the nodes to inject artificial latency and jitter: on the downlink fronthaul link ($T_2-T_1$) and on the uplink MAC feedback link ($T_5-T_4$).
\item \textbf{Experimental Scenario II (Near-Site Single-Hop Deployment):} A near-site campus deployment where the VNF and PNF are co-located in the same campus area and connected via a single commercial Ethernet switch, introducing minimal base transport latency (around 0.3--0.5~ms). This scenario, together with Scenario III, is deployed in the campus network topology illustrated in Figure~\ref{fig:scenario2_topology}. It evaluates the proposed timing control under stable, low-latency fronthaul conditions.
\item \textbf{Experimental Scenario III (Multi-Switch/Router Campus Deployment):} A realistic multi-hop campus network topology where the VNF and PNF are physically disaggregated across different campus buildings. As shown in Figure~\ref{fig:scenario2_topology}, the connection traverses $3$ commercial switches and $3$ commercial routers, introducing realistic transport delays and route-dependent jitter (ping latency up to 2.0~ms).
\end{itemize}

The detailed hardware and software configuration of the end-to-end network deployment in the testbed environment is shown in the table below.

\begin{table}[!htbp]
\iffalse
  \caption{測試平台節點配置}
\fi
  \caption{Testbed Node Configuration}
  \label{tab:node_config}
  \centering
  \begin{tabularx}{\textwidth}{l|L}
    \toprule
    \textbf{Unit} & \textbf{Specification} \\
    \midrule
    Core Network (CN) & Open5GS \cite{Open5GS} \\
    Software Protocol Stack / Branch & OpenAirInterface \cite{OAI} (\texttt{2026.w13}) \\
    VNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6226R \cite{IntelXeon622R} with NetXtreme BCM5719 NIC \cite{NetXtreme_BCM5719, nic_interface} \\
    PNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6433N \cite{IntelXeon6433N} with Intel I210 NIC \cite{Intel_I210, nic_interface} \\
    O-RU & Pegatron RU (P5G-Indoor O-RU-PR1450) \\
    Commercial Off-The-Shelf (COTS) UE & Samsung Galaxy S20 \cite{3GPP.TS.38.101} \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[!htbp]
\iffalse
\caption{無線系統參數}
\fi
\caption{Wireless System Parameters}
\label{tab:wireless_params}
\centering
\begin{tabular}{l|l}
\toprule
\textbf{Parameter} & \textbf{Setting} \\
\midrule
Subcarrier Spacing & 30 kHz \\
TDD Pattern & 3D1S1U \\
MIMO / Ports & 4-layer / 4T4R \\
Fronthaul Interface & O-RAN F release \\
\bottomrule
\end{tabular}
\end{table}

To ensure consistent data quality for automated measurement and analysis, this study developed an automated evaluation rApp running on the Non-Real-Time RAN Intelligent Controller (Non-RT RIC) within the O-RAN Service Management and Orchestration (SMO) framework \cite{O-RAN.arch, O-RAN.rapp}, as shown in Figure~\ref{fig:rapp_architecture}. This setup serves as the testbed architecture for Experimental Scenario I. This rApp automatically starts the VNF and PNF. Once the gNB is successfully established, the rApp automatically connects to the control PC via SSH, toggles the UE's airplane mode using the Android Debug Bridge (ADB) \cite{ADB}, and then launches an iPerf \cite{iperf3} server on the Core Network server via SSH. Subsequently, it automatically executes a series of predefined iPerf test cases. After that, the rApp collects all log files via O1 SFTP and uses Python scripts to automatically plot charts for subsequent data analysis. During the experiments, we also used tcpdump \cite{tcpdump} for packet sniffing, and utilized Linux PTP \cite{LinuxPTP311} and TSN time synchronization mechanisms \cite{tsn-timesync} to ensure baseline timing among network nodes, while underlying data transmission relied on DPDK \cite{DPDK20119} for packet acceleration. A key capability of this framework is the ability to automate network delay simulations by running Linux Traffic Control (TC) on the intermediate link. Specifically, Linux TC is applied to inject latency and jitter on the fronthaul link ($T_2-T_1$) and on the MAC feedback link ($T_5-T_4$), which is crucial for verifying the stability of the disaggregated architecture under non-deterministic latency.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figures/rapp.png}
\iffalse
  \caption{rApp 自動化評估框架架構。}
\fi
  \caption{Architecture of the rApp automated evaluation framework for Experimental Scenario I (Controlled Single-Switch setup with Linux TC delay injection).}
  \label{fig:rapp_architecture}
\end{figure}

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/Experimental_Scenario.png}
  \caption{Experimental Scenario II and III: campus network deployments (near-site single-hop and multi-switch/router multi-hop) for evaluating the proposed adaptive timing control under realistic transport.}
  \label{fig:scenario2_topology}
\end{figure*}

\subsection{Thorough System-Level Integration and Validation}
\label{subsec:software_validation}

To prove the stability, practical deployability, and compatibility of the proposed timing control and nFAPI modifications, our software implementation was submitted and successfully merged into the official upstream OpenAirInterface (OAI) repository \cite{nikaein2014openairinterface}. Beyond our localized end-to-end evaluation using the rApp framework, the proposed solution went through thorough testing in the official OAI Continuous Integration and Continuous Deployment (CI/CD) pipeline \cite{oai_gitlab_ci}. This automated pipeline executes multi-platform builds and physical layer regression suites to ensure production-grade software quality.

In the software compilation and image deployment stage, the proposed implementation demonstrates high portability across heterogeneous hardware architectures. As compiled in Table~\ref{tab:oai_cicd_build}, the cross-platform builds successfully passed on standard x86 servers, ARM64-based edge servers, and NVIDIA Jetson edge AI platforms under both Ubuntu and enterprise-grade Red Hat Enterprise Linux (RHEL) \cite{redhat_rhel} environments. These results confirm that our proposed timing control is independent of specific hardware acceleration cards, allowing seamless deployment on general-purpose Commercial Off-The-Shelf (COTS) cloud infrastructure.

\begin{table}[!htbp]
  \caption{OAI CI/CD Cross-Platform Compilation and Image Building Results}
  \label{tab:oai_cicd_build}
  \centering
  \small
  \begin{tabularx}{\textwidth}{l|>{\hsize=0.8\hsize}L|>{\hsize=1.2\hsize}L|c}
    \toprule
    \textbf{Stage} & \textbf{Job / Builder} & \textbf{Target and Hardware Description} & \textbf{Result} \\
    \midrule
    x86 Build & Ubuntu-Image-Builder & Build OAI network function images on standard x86 servers & Passed \\
    ARM Build & Ubuntu-ARM-Image-Builder & Build images on ARM64 for ARM-based edge servers & Passed \\
    Edge AI Build & Ubuntu-Jetson-Image-Builder & Build light-weight images for NVIDIA Jetson platforms & Passed \\
    RHEL Build & RHEL-Cluster-Image-Builder & Build images for Red Hat Enterprise Linux (RHEL) clusters & Passed \\
    Cross-Compile & ARM-Cross-Compile & Verify cross-compilation from x86 to ARM64 architecture & Passed \\
    \bottomrule
  \end{tabularx}
\end{table}

Following cross-platform compilation, our implementation was subjected to system integration and physical-layer dynamic testing. Table~\ref{tab:oai_integration_tests} summarizes the key integration and simulation tests that our code passed. The validation demonstrates that the slot-level scheduler adjustments ($S_{ahead}$) and microsecond-level clock corrections ($S_{adj}, \mu s_{adj}$) do not break 3GPP-compliant physical layer procedures \cite{3gpp_ts_38_212} or F1/N2 standard interface control-plane handovers \cite{3gpp_ts_38_473}. Crucially, the code successfully integrated with commercial Quectel modules under standard RF conditions, proving that the proposed software-defined timing loops successfully satisfy the strict timing window constraints of real-world physical basebands.

\begin{table}[!htbp]
  \caption{Summary of System Integration and Physical Layer Validation}
  \label{tab:oai_integration_tests}
  \centering
  \small
  \begin{tabularx}{\textwidth}{>{\hsize=0.75\hsize}L|>{\hsize=0.85\hsize}L|>{\hsize=1.4\hsize}L}
    \toprule
    \textbf{Validation Scope} & \textbf{Passed Job Name} & \textbf{Description and Highlights} \\
    \midrule
    E2E 5G SA and RIC Integration & \texttt{OAI-\allowbreak FLEXRIC-\allowbreak RAN-\allowbreak Integration} & Run in 5G SA mode, integrate with 5G Core and FlexRIC (near-RT RIC) \cite{schmidt2021flexric} with low packet loss. This validation uses the E2 interface protocol \cite{oran_wg3_e2ap}. \\
    Commercial UE and RF Integration & \texttt{RAN-\allowbreak NSA-\allowbreak B200-\allowbreak Module-\allowbreak LTEBOX} \newline \texttt{RAN-\allowbreak SA-\allowbreak B200-\allowbreak Module-\allowbreak SABOX} & Run with USRP B200 and commercial Quectel modules to meet strict timing in real RF environments. \\
    Third-Party RF Frontend Compatibility & \texttt{RAN-\allowbreak SA-\allowbreak AW2S-\allowbreak CN5G} & Run end-to-end tests with third-party AW2S RF equipment (20MHz TDD SA) \cite{aw2s_rf_manual} and Amarisoft UE. \\
    Handover Stability & \texttt{RAN-\allowbreak SA-\allowbreak Handover-\allowbreak CN5G} & Pass F1-interface and N2-interface handover tests \cite{3gpp_ts_38_473} under 5G SA to keep control plane stable. \\
    5G L1 and Channel Compatibility & \texttt{RAN-\allowbreak Channel-\allowbreak Simulation} \newline \texttt{RAN-\allowbreak PhySim-\allowbreak GraceHopper-\allowbreak 5G} & Pass physical layer simulations (including LDPC, Polar, PBCH, MIMO, MCS) to prove timing changes do not break 3GPP rules \cite{3gpp_ts_38_212}. Testing was also performed on GPU-accelerated platforms \cite{nvidia_grace_hopper_oai}. \\
    \bottomrule
  \end{tabularx}
\end{table}

These regression and compilation results prove that the proposed dynamic delay-aware timing control is robust, backward-compatible, and fully standardized, serving as a stable baseline for future O-RAN Option~6 deployments in the global open-source community.

\iffalse
\section{實驗一：PNF Timing Info ($\mathit{TimingInfo}[i]$) 之 EWMA 驗證}
\fi
\section{Experiment 1: Validation of EWMA for PNF Timing Info}
\label{subsec:scenario_1}

This scenario verifies the effectiveness of applying the Exponentially Weighted Moving Average (EWMA) to smooth the PNF Timing Info ($\mathit{TimingInfo}[i]$). Due to network jitter and processing delays, directly using the raw PNF Timing Info can lead to unstable scheduling decisions. Figure~\ref{fig:exp1a} shows the timing control directly using the raw PNF Timing Info ($\mathit{TimingInfo}[i]$), where the raw timing values change a lot. Figure~\ref{fig:exp1b} represents the cumulative count of the packets that exceed the deadline when the timing adjustment directly uses the raw PNF Timing Info ($\mathit{TimingInfo}[i]$). The EWMA mechanism filters out the high-frequency jitter, as shown in the PNF Timing Info ($\mathit{TimingInfo}[i]$) after applying EWMA in Figure~\ref{fig:exp1c}. Finally, Figure~\ref{fig:exp1d} shows the cumulative count of the packets exceeding the deadline under the EWMA-based timing control. The results show that applying EWMA allows the timing loop to adjust the slots ahead stably, which greatly reduces the number of packets that arrive after the deadline.

\begin{figure}[!htbp]
  \centering
\iffalse
  \subfloat[{直接使用 \(\mathit{TimingInfo}[i]\)}]{\includegraphics[width=0.65\textwidth]{figures/exp1a.png}\label{fig:exp1a}} \\
\fi
  \subfloat[{Direct Use of \(\mathit{TimingInfo}[i]\)}]{\includegraphics[width=0.65\textwidth]{figures/exp1a.png}\label{fig:exp1a}} \\
  \vspace{0.15cm}
\iffalse
  \subfloat[{直接使用 \(\mathit{TimingInfo}[i]\) 調整下超過 Deadline 封包之累計數量}]{\includegraphics[width=0.65\textwidth]{figures/exp1b.png}\label{fig:exp1b}}
\fi
  \subfloat[Cumulative Count of Packets Exceeding Deadline under Direct Use]{\includegraphics[width=0.65\textwidth]{figures/exp1b.png}\label{fig:exp1b}}
\iffalse
  \caption{PNF Timing Info ($\mathit{TimingInfo}[i]$) 之 EWMA 處理驗證。}
\fi
  \caption{EWMA processing validation for PNF Timing Info ($\mathit{TimingInfo}[i]$).}
  \label{fig:exp1}
\end{figure}

\clearpage

\begin{figure}[!htbp]
  \ContinuedFloat
  \centering
\iffalse
  \subfloat[{套用 EWMA 之 \(\mathit{TimingInfo}[i]\)}]{\includegraphics[width=0.65\textwidth]{figures/exp1c.png}\label{fig:exp1c}} \\
\fi
  \subfloat[{EWMA Application on \(\mathit{TimingInfo}[i]\)}]{\includegraphics[width=0.65\textwidth]{figures/exp1c.png}\label{fig:exp1c}} \\
  \vspace{0.15cm}
\iffalse
  \subfloat[套用 EWMA 調整下超過 Deadline 封包之累計數量]{\includegraphics[width=0.65\textwidth]{figures/exp1d.png}\label{fig:exp1d}}
\fi
  \subfloat[Cumulative Count of Packets Exceeding Deadline under EWMA]{\includegraphics[width=0.65\textwidth]{figures/exp1d.png}\label{fig:exp1d}}
\iffalse
  \caption[]{PNF Timing Info ($\mathit{TimingInfo}[i]$) 之 EWMA 處理驗證（續）。}
\fi
  \caption[]{EWMA processing validation for PNF Timing Info ($\mathit{TimingInfo}[i]$) (cont.).}
\end{figure}

\iffalse
\section{實驗二：性能基準測試}
\fi
\section{Experiment 2: Throughput \& MAC RTT efficiency against static baselines}
\label{subsec:scenario_2}

In this scenario, we evaluate the benchmark performance of the proposed adaptive timing mechanism against a standard static slots ahead configuration. Figure~\ref{fig:exp2} presents the performance evaluation in terms of MAC layer latency and system throughput.

Figure~\ref{fig:exp2} displays the performance evaluation under different offered loads.
The proposed method aims to sustain the target throughput across varying offered loads while keeping the average MAC HARQ RTT
as low as possible.
As shown in Figure~\ref{fig:exp2a}, when the offered load is low (between 100~Mbps and 800~Mbps), all configurations except
2 slots ahead can track the offered load and sustain the target throughput.
However, when the offered load increases to 1000~Mbps, static configurations with smaller slots ahead settings fail to
maintain the throughput: the 4 slots ahead configuration levels off at about 830~Mbps, and the 6 slots ahead configuration
levels off at about 870~Mbps.
The 2 slots ahead configuration drops to near-zero throughput when the offered load goes beyond 200~Mbps because the fixed
timing advance is too small.
Although larger static configurations like 8 slots ahead can sustain the throughput at 940~Mbps under high load, they
introduce larger RTT latency across all loads, as shown in Figure~\ref{fig:exp2b}.
The proposed method dynamically adjusts the slots ahead based on the feedback.
For offered loads from 100~Mbps to 700~Mbps, it selects smaller slots ahead settings, maintaining a lower average MAC HARQ RTT
of about 5.8~ms to 6.7~ms, which is lower than the 4 slots ahead configuration (about 7.0~ms).
When the offered load increases to 1000~Mbps, the proposed method dynamically increases the scheduling advance to sustain
the target throughput.
This increase causes the average MAC HARQ RTT to rise to about 7.9~ms at 1000~Mbps.
However, this RTT is still lower than that of static 6 slots ahead (about 8.4~ms), 8 slots ahead (about 9.2~ms), and
10 slots ahead (about 10.6~ms) configurations.
These results show that the proposed method effectively minimizes RTT overhead while sustaining the target throughput
under different offered loads.

\begin{figure}[!htbp]
  \centering
\iffalse
  \subfloat[吞吐量優化]{\includegraphics[width=0.7\textwidth]{figures/exp2a.png}\label{fig:exp2a}} \\
\fi
  \subfloat[Throughput Optimization]{\includegraphics[width=0.7\textwidth]{figures/exp2a.png}\label{fig:exp2a}} \\
  \vspace{0.5cm}
\iffalse
  \subfloat[MAC 延遲與可靠性]{\includegraphics[width=0.7\textwidth]{figures/exp2b.png}\label{fig:exp2b}}
\fi
  \subfloat[MAC Latency and Reliability]{\includegraphics[width=0.7\textwidth]{figures/exp2b.png}\label{fig:exp2b}}
  \caption{Performance Evaluation: MAC RTT Efficiency and System Throughput.}
  \label{fig:exp2}
\end{figure}

\iffalse
\section{實驗三：網路擁塞下的穩定性}
\fi
\section{Experiment 3: Stability under Traffic Control Network Congestion}
\label{subsec:scenario_3}

To test the system's stability, we utilized Linux TC to inject artificial network congestion into the $T_2-T_1$ and $T_5-T_4$ segments, altering the One-Way Delay (OWD) and jitter. As shown in Figure~\ref{fig:exp3a}, when the delay exceeds the fixed timing window, the static configuration experiences a severe drop in throughput and frequent connection resets. In contrast, the proposed adaptive timing control dynamically expands the slots ahead ($S_{ahead}$). This expansion allows the MAC scheduler to trigger messages in advance, ensuring that messages can still arrive at the PNF within the valid timing window despite the increased transmission delay. Therefore, even at congestion levels that would previously cause complete synchronization failure, the system can maintain stable throughput. Specifically, the proposed method maintains a throughput above 900~Mbps across all tested VNF-PNF latency values up to 3.0~ms, whereas the best-performing static configuration (10~slots ahead) degrades to near-zero throughput at approximately 2.5~ms injected latency.

To further evaluate the impact of unpredictable network jitter between the VNF and PNF, we perform a second experiment using Linux TC to simulate random jitter on the fronthaul link. In this test, we run iPerf UDP traffic with an offered load of 1~Gbps to fully saturate the downlink transmission. We measure the resulting downlink (DL) receiver (RX) throughput at the UE, comparing different fixed slots ahead configurations against our proposed adaptive timing mechanism. As shown in Figure~\ref{fig:exp3b}, when network jitter is high and unpredictable, fixed slots ahead settings cannot handle the timing variations, leading to packet losses, sync failures, and a major drop in throughput. On the other hand, the proposed adaptive method dynamically adjusts the scheduling advance, keeping the transmission timing aligned. As a result, the UE achieves and maintains stable and high DL RX throughput even under severe jitter conditions. Quantitatively, the proposed method exhibits graceful throughput degradation, maintaining over 900~Mbps at 0--600~$\mu$s jitter and approximately 750~Mbps even at 1000~$\mu$s jitter. In contrast, static configurations collapse sharply at their respective thresholds: the best-performing static configuration (6~slots ahead) drops below 300~Mbps at 800~$\mu$s and near zero at 1000~$\mu$s, while configurations with 4, 8, and 10~slots ahead all fail below 600~$\mu$s.

\begin{figure}[!htbp]
  \centering
\iffalse
  \subfloat[不同網路擁塞程度下的系統穩定性]{\includegraphics[width=0.7\textwidth]{figures/exp3a.png}\label{fig:exp3a}} \\
\fi
  \subfloat[System Stability under Network Congestion]{\includegraphics[width=0.7\textwidth]{figures/exp3a.png}\label{fig:exp3a}} \\
  \vspace{0.5cm}
\iffalse
  \subfloat[無法預測抖動下的吞吐量表現]{\includegraphics[width=0.7\textwidth]{figures/exp3b.png}\label{fig:exp3b}}
\fi
  \subfloat[Throughput Performance under Unpredictable Jitter]{\includegraphics[width=0.7\textwidth]{figures/exp3b.png}\label{fig:exp3b}}
  \caption{System stability and throughput performance under network congestion and jitter.}
  \label{fig:exp3}
\end{figure}

\section{Experiment 4: Synchronization Offset}
\label{subsec:scenario_4}

To evaluate the synchronization performance of the proposed node sync mechanism under a multi-hop transport network, we deploy the VNF and PNF across the campus network in Experimental Scenario III. As shown in Fig.~\ref{fig:scenario2_topology}, the VNF-side components are placed at a remote computing site, while the radio-side components and UE remain at the local radio site. Fig.~\ref{fig:exp4} shows the measured VNF-PNF synchronization offset in microseconds over elapsed slots.

When the connection between the VNF and PNF is established, they start calculating slot 0 independently. Program start-up timing differences cause an initial program execution timing offset. In addition, their clock phase drifts during operation. When the calculated offset accumulates and becomes larger than one slot (500~$\mu$s for subcarrier spacing of 30~kHz and slot duration of 500~$\mu$s), the system triggers the node sync algorithm to adjust the VNF's scheduling time base. This readjusts the VNF's slot boundaries to align with the PNF, correcting the time-base offset and stabilizing the synchronization offset within a small range of 0 to 100~$\mu$s. This result confirms that the proposed node sync algorithm can dynamically correct the time-base offset and maintain high synchronization accuracy even under a realistic, non-deterministic campus network environment.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/exp4.png}
  \caption{VNF-PNF synchronization offset over elapsed slots under the multi-switch/router deployment.}
  \label{fig:exp4}
\end{figure}


\section{Experiment 5: Ping Latency under Different Throughput}
\label{subsec:scenario_5}

To evaluate the system-level throughput stability of the proposed timing-control mechanism under different real deployment conditions, we execute end-to-end data transmission under Experimental Scenario II and Experimental Scenario III. We use ping to measure the network interface latency between the VNF and PNF. Under Scenario II (Near-site deployment, Fig.~\ref{fig:exp5}(a)), the base latency is lower and more stable (around 0.3 to 0.5~ms). As illustrated in the metrics-specific line charts of Fig.~\ref{fig:exp5}(a), which effectively illustrate the performance trade-offs, the proposed method maintains stable near-peak throughput, and because the transport delay is lower, the required slots ahead parameter is smaller, which also reduces the MAC HARQ RTT. Under Scenario III (Remote deployment, Fig.~\ref{fig:exp5}(b)), the connection spans the campus network and traverses multiple switches and routers, resulting in higher base latency and queueing delay variations (ping latency up to 2.0~ms). Despite this severe transport jitter, the proposed method dynamically adapts the slots ahead parameter to prevent late packet arrivals, maintaining stable data transmission.

\begin{figure}[!htbp]
  \centering
  \subfloat[Scenario II: Near-site deployment]{\includegraphics[width=0.95\textwidth]{figures/exp5a.png}\label{fig:exp5a}} \\
  \vspace{0.3cm}
  \subfloat[Scenario III: Remote deployment]{\includegraphics[width=0.95\textwidth]{figures/exp5b.png}\label{fig:exp5b}}
  \caption{UE RX throughput and ping latency under different real network deployment scenarios.}
  \label{fig:exp5}
\end{figure}

\section{Experiment 6: Timing Info Mode}
\label{subsec:scenario_6}

We evaluate the impact of different timing info reporting modes and periods under Scenario II (Near-site deployment, Fig.~\ref{fig:exp6}(a)) and Scenario III (Remote deployment, Fig.~\ref{fig:exp6}(b)). The nFAPI protocol supports periodic and aperiodic timing info feedback modes. Fig.~\ref{fig:exp6}(a) and Fig.~\ref{fig:exp6}(b) present the packet loss rate under different reporting configurations for both deployment scenarios. In both cases, increasing the timing period in periodic mode leads to a higher packet loss rate. This is because a longer period reduces feedback frequency. As a result, the VNF timing loop cannot adjust the slots ahead parameter fast enough to adapt to network variations, causing more slot packets to arrive late and be discarded by the PNF. Under Scenario III (Remote deployment), the longer distance and higher transport jitter result in a higher base packet loss rate, making the system more sensitive to feedback delay. Under Scenario II (Near-site deployment), the lower base latency and jitter allow the system to maintain a lower packet loss rate even with a slightly longer timing period.

\begin{figure}[!htbp]
  \centering
  \subfloat[Scenario II: Near-site deployment]{\includegraphics[width=0.95\textwidth]{figures/exp6a.png}\label{fig:exp6a}} \\
  \vspace{0.3cm}
  \subfloat[Scenario III: Remote deployment]{\includegraphics[width=0.95\textwidth]{figures/exp6b.png}\label{fig:exp6b}}
  \caption{Evaluation of packet loss rate under different deployment scenarios.}
  \label{fig:exp6}
\end{figure}

\end{document}
