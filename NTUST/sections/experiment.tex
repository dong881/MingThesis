\documentclass[../my_ntust_thesis.tex]{subfiles}
\begin{document}
\iffalse
\chapter{實驗結果}
\fi
\chapter{Experimental Results}
\label{sec:experiment}

This study conducts experiments in an OpenAirInterface (OAI) nFAPI end-to-end environment to verify the proposed delay management method. To ensure the statistical reliability of the evaluation, each throughput value reported represents the average of 180 data points (3 independent trials $\times$ 60 samples per trial).

To validate the hypothesis and the effectiveness of the proposed dynamic trigger scheduling algorithm, the experimental scenarios are arranged as follows: Section~\ref{subsec:experimental_scenario} outlines the hardware and software environment as well as the automated evaluation framework. Section~\ref{subsec:scenario_1} validates the Exponentially Weighted Moving Average (EWMA) mechanism for the PNF arrival offset ($\Delta t_{arrive}$). Section~\ref{subsec:scenario_2} benchmarks the performance of peak throughput and MAC layer Round-Trip Time (RTT) against static baselines. Section~\ref{subsec:scenario_3} evaluates the stability of the proposed method under varying degrees of network congestion.

\iffalse
\section{實驗情境}
\fi
\section{Experimental Scenario}
\label{subsec:experimental_scenario}

The testbed environment connects a series of high-performance servers, radio units, and commercial smartphones. The detailed hardware and software configuration of the end-to-end network deployment is shown in the table below.

\begin{table}[!htbp]
\iffalse
  \caption{測試平台節點配置}
\fi
  \caption{Testbed Node Configuration}
  \label{tab:node_config}
  \centering
  \begin{tabularx}{\textwidth}{l|L}
    \toprule
    \textbf{Unit} & \textbf{Specification} \\
    \midrule
    Core Network (CN) & Open5GS \cite{Open5GS} \\
    Software Protocol Stack / Branch & OpenAirInterface \cite{OAI} (\texttt{2026.w13}) \\
    VNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6226R \cite{IntelXeon622R} with NetXtreme BCM5719 NIC \cite{NetXtreme_BCM5719, nic_interface} \\
    PNF Server & Intel\textsuperscript{\tiny\textregistered} Xeon\textsuperscript{\tiny\textregistered} Gold 6433N \cite{IntelXeon6433N} with Intel I210 NIC \cite{Intel_I210, nic_interface} \\
    O-RU & Pegatron RU (P5G-Indoor O-RU-PR1450) \\
    Commercial Off-The-Shelf (COTS) UE & Samsung Galaxy S20 \cite{3GPP.TS.38.101} \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[!htbp]
\iffalse
\caption{無線系統參數}
\fi
\caption{Wireless System Parameters}
\label{tab:wireless_params}
\centering
\begin{tabular}{l|l}
\toprule
\textbf{Parameter} & \textbf{Setting} \\
\midrule
Subcarrier Spacing & 30 kHz \\
TDD Pattern & 3D1S1U \\
MIMO / Ports & 4-layer / 4T4R \\
Fronthaul Interface & O-RAN F release \\
\bottomrule
\end{tabular}
\end{table}

To ensure consistent data quality for automated measurement and analysis, this study developed an automated evaluation rApp running on the Non-Real-Time RAN Intelligent Controller (Non-RT RIC) within the O-RAN Service Management and Orchestration (SMO) framework \cite{O-RAN.arch, O-RAN.rapp}, as shown in Figure~\ref{fig:rapp_architecture}. This rApp automatically starts the VNF and PNF. Once the gNB is successfully established, the rApp automatically connects to the control PC via SSH, toggles the UE's airplane mode using the Android Debug Bridge (ADB) \cite{ADB}, and then launches an iPerf \cite{iperf3} server on the Core Network server via SSH. Subsequently, it automatically executes a series of predefined iPerf test cases. After that, the rApp collects all log files via O1 SFTP and uses Python scripts to automatically plot charts for subsequent data analysis. During the experiments, we also used tcpdump \cite{tcpdump} for packet sniffing, and utilized Linux PTP \cite{LinuxPTP311} and TSN time synchronization mechanisms \cite{tsn-timesync} to ensure baseline timing among network nodes, while underlying data transmission relied on DPDK \cite{DPDK20119} for packet acceleration. A key capability of this framework is the ability to automate network delay simulations on the MAC feedback link ($T_5-T_4$) and fronthaul link ($T_2-T_1$) using Linux Traffic Control (TC), which is crucial for verifying the stability of the disaggregated architecture under non-deterministic latency.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figures/rapp.png}
\iffalse
  \caption{rApp 自動化評估框架架構。}
\fi
  \caption{Architecture of the rApp automated evaluation framework.}
  \label{fig:rapp_architecture}
\end{figure}

\iffalse
\section{情境 I：PNF 到達偏移量 ($\Delta t_{arrive}$) 之 EWMA 驗證}
\fi
\section{Scenario I: EWMA Validation for PNF Arrival Offset ($\Delta t_{arrive}$)}
\label{subsec:scenario_1}

This scenario verifies the effectiveness of applying the Exponentially Weighted Moving Average (EWMA) to smooth the PNF arrival offset ($\Delta t_{arrive}$). Due to network jitter and processing delays, the raw timing offset received from the PNF can exhibit high variability. Figure~\ref{fig:exp1} illustrates the impact of EWMA processing. Without EWMA (Figure~\ref{fig:exp1a}), the arrival offset fluctuates drastically, which can lead to unstable scheduling decisions. By applying EWMA (Figure~\ref{fig:exp1b}), high-frequency jitter is effectively filtered, providing a stable baseline for the timing adjustment algorithm. Furthermore, as shown in Figures~\ref{fig:exp1c} and \ref{fig:exp1d}, the smoothed data allows the system to make relatively stable judgments. When $\Delta t_{arrive}$ begins to show a downward trend, the algorithm steadily identifies this trend and actively advances more time ($S_{ahead}$) to maintain synchronization stability.

\begin{figure}[!htbp]
  \centering
\iffalse
  \subfloat[未應用 EWMA]{\includegraphics[width=0.65\textwidth]{figures/exp1a.png}\label{fig:exp1a}} \\
\fi
  \subfloat[Without EWMA]{\includegraphics[width=0.65\textwidth]{figures/exp1a.png}\label{fig:exp1a}} \\
  \vspace{0.15cm}
\iffalse
  \subfloat[已應用 EWMA]{\includegraphics[width=0.65\textwidth]{figures/exp1b.png}\label{fig:exp1b}}
\fi
  \subfloat[With EWMA]{\includegraphics[width=0.65\textwidth]{figures/exp1b.png}\label{fig:exp1b}}
\iffalse
  \caption{PNF 到達偏移量 ($\Delta t_{arrive}$) 之 EWMA 處理驗證與主動調整。}
\fi
  \caption{EWMA processing validation and active adjustment for PNF arrival offset ($\Delta t_{arrive}$).}
  \label{fig:exp1}
\end{figure}

\clearpage

\begin{figure}[!htbp]
  \ContinuedFloat
  \centering
\iffalse
  \subfloat[趨勢觀察]{\includegraphics[width=0.65\textwidth]{figures/exp1c.png}\label{fig:exp1c}} \\
\fi
  \subfloat[Trend Observation]{\includegraphics[width=0.65\textwidth]{figures/exp1c.png}\label{fig:exp1c}} \\
  \vspace{0.15cm}
\iffalse
  \subfloat[主動提前調整]{\includegraphics[width=0.65\textwidth]{figures/exp1d.png}\label{fig:exp1d}}
\fi
  \subfloat[Active Advance Adjustment]{\includegraphics[width=0.65\textwidth]{figures/exp1d.png}\label{fig:exp1d}}
\iffalse
  \caption{PNF 到達偏移量 ($\Delta t_{arrive}$) 之 EWMA 處理驗證與主動調整（續）。}
\fi
  \caption{EWMA processing validation and active adjustment for PNF arrival offset ($\Delta t_{arrive}$) (cont.).}
\end{figure}

\iffalse
\section{情境 II：性能基準測試}
\fi
\section{Scenario II: Performance Benchmarking}
\label{subsec:scenario_2}

In this scenario, we evaluate the benchmark performance of the proposed adaptive timing mechanism against a standard static slots ahead configuration. Figure~\ref{fig:exp2} presents the performance evaluation in terms of MAC layer latency and system throughput.

Figure~\ref{fig:exp2} displays the performance evaluation under varying Offered Load. The proposed algorithm prioritizes ensuring optimal throughput, consistently maintaining optimal throughput performance under all load conditions. Simultaneously, while ensuring this optimal throughput, the algorithm maximizes the selection of the optimal slots ahead to optimize the MAC layer HARQ Round-Trip Time (RTT), effectively avoiding unnecessary extra overhead.

\begin{figure}[!htbp]
  \centering
\iffalse
  \subfloat[MAC 延遲與可靠性]{\includegraphics[width=0.7\textwidth]{figures/exp2a.png}\label{fig:exp2a}} \\
\fi
  \subfloat[MAC Latency and Reliability]{\includegraphics[width=0.7\textwidth]{figures/exp2a.png}\label{fig:exp2a}} \\
  \vspace{0.5cm}
\iffalse
  \subfloat[吞吐量優化]{\includegraphics[width=0.7\textwidth]{figures/exp2b.png}\label{fig:exp2b}}
\fi
  \subfloat[Throughput Optimization]{\includegraphics[width=0.7\textwidth]{figures/exp2b.png}\label{fig:exp2b}}
\iffalse
  \caption{性能評估：MAC RTT 效率與系統吞吐量。}
\fi
  \caption{Performance Evaluation: MAC RTT Efficiency and System Throughput.}
  \label{fig:exp2}
\end{figure}

\iffalse
\section{情境 III：網路擁塞下的穩定性}
\fi
\section{Scenario III: Stability under Network Congestion}
\label{subsec:scenario_3}

To test the system's robustness, we utilized Linux TC to inject artificial network congestion into the $T_2-T_1$ and $T_5-T_4$ segments, altering the One-Way Delay (OWD) and jitter. As shown in Figure~\ref{fig:exp3}, when the delay exceeds the fixed timing window, the static configuration experiences a severe drop in throughput and frequent connection resets. In contrast, the proposed adaptive timing control dynamically expands the slots ahead ($S_{ahead}$). This expansion allows the MAC scheduler to trigger messages in advance, ensuring that messages can still arrive at the PNF within the valid timing window despite the increased transmission delay. Consequently, even at congestion levels that would previously cause complete synchronization failure, the system can maintain stable throughput.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figures/exp3.png}
\iffalse
  \caption{不同網路擁塞程度下的系統穩定性與吞吐量表現。}
\fi
  \caption{System stability and throughput performance under varying degrees of network congestion.}
  \label{fig:exp3}
\end{figure}

\end{document}
